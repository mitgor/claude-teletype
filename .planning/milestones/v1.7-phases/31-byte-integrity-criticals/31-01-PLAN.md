---
phase: 31-byte-integrity-criticals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/claude_teletype/printing/drivers.py
  - tests/test_printer.py
  - tests/test_byte_integrity.py
autonomous: true
requirements: [BYTE-01, BYTE-02, BYTE-03]
must_haves:
  truths:
    - "A ppds codepage_command ending 0xb5 reaches the inner driver verbatim — _send_raw never decodes bytes to str"
    - "cp437/cp866 encoded text sent over CupsPrinterDriver arrives at the lp subprocess with all bytes >= 0x80 intact"
    - "A regression test round-trips a 0xb5-bearing sequence through both ProfilePrinterDriver and CupsPrinterDriver and fails if any byte is altered"
  artifacts:
    - path: "src/claude_teletype/printing/drivers.py"
      provides: "byte-clean _send_raw and bytes-based CupsPrinterDriver line buffer"
      contains: "self._inner.write_bytes(data)"
    - path: "tests/test_byte_integrity.py"
      provides: "BYTE-03 contract-lock regression test"
      min_lines: 40
  key_links:
    - from: "src/claude_teletype/printing/drivers.py"
      to: "inner PrinterDriver.write_bytes"
      via: "ProfilePrinterDriver._send_raw"
      pattern: "_inner\\.write_bytes\\(data\\)"
    - from: "src/claude_teletype/printing/drivers.py"
      to: "subprocess.run lp"
      via: "CupsPrinterDriver._flush_line bytes join"
      pattern: "b\"\"\\.join"
---

<objective>
Fix the two critical ASCII round-trip corruptions in the driver layer (CR-01, CR-02) and lock the fix in with a byte-integrity regression test (BYTE-03).

Purpose: Any byte >= 0x80 in codepage commands, style sequences, or cp437/cp866 text is currently mangled to `?` on the ProfilePrinterDriver raw path and the CUPS path — silently breaking the codepage feature v1.6 shipped. 905 tests missed it because no test carries a high-byte payload end-to-end.

Output: Corrected `drivers.py`, updated `tests/test_printer.py` helpers/assertions, new `tests/test_byte_integrity.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v1.6-REVIEW.md
@src/claude_teletype/printing/drivers.py
</context>

<interfaces>
From src/claude_teletype/printing/drivers.py (current state — the bugs to fix):

```python
class PrinterDriver(Protocol):
    def write(self, char: str) -> None: ...
    def write_bytes(self, data: bytes) -> None: ...   # raw channel, preserves bytes

# CR-01 (drivers.py:247-255): str round-trip destroys bytes >= 0x80
def _send_raw(self, data: bytes) -> None:
    if data:
        self._inner.write(data.decode("ascii", errors="replace"))

# CR-02 (drivers.py:105-153): str line buffer + double ASCII replace
class CupsPrinterDriver:
    self._line_buffer: list[str]
    # write_bytes decodes to str; _flush_line re-encodes with errors="replace"
```

From src/claude_teletype/printing/profiles.py:215 — the concrete corruption case:
`ppds` profile `codepage_command = b"\x1b[T\x04\x00\x00\x00\x01\xb5"` (trailing 0xb5 becomes 0x3f on the wire today).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing byte-integrity regression test (BYTE-03)</name>
  <files>tests/test_byte_integrity.py</files>
  <read_first>
    - .planning/v1.6-REVIEW.md (sections CR-01 and CR-02 — exact corruption mechanics and byte values)
    - src/claude_teletype/printing/drivers.py (ProfilePrinterDriver._send_raw, CupsPrinterDriver.write_bytes/_flush_line, PrinterDriver protocol)
    - src/claude_teletype/printing/profiles.py (ppds profile definition — codepage_command bytes; get_profile)
    - tests/test_printer.py (existing conventions: MagicMock inner drivers, `patch("claude_teletype.printing.drivers.subprocess.run")` canonical patch path)
  </read_first>
  <behavior>
    - Test 1 (ProfilePrinterDriver raw path): construct ProfilePrinterDriver over a MagicMock inner (is_connected=True), call write_bytes with the ppds codepage_command b"\x1b[T\x04\x00\x00\x00\x01\xb5". Assert the inner driver received those exact 9 bytes via write_bytes — including the trailing 0xb5 — and that inner.write was NOT used to carry them.
    - Test 2 (ProfilePrinterDriver codepage text path): using get_profile("ppds"), write a non-ASCII char that cp437 encodes above 0x7F (e.g. "─" → 0xc4). Assert the codepage_command bytes AND the 0xc4 text byte reach the inner verbatim.
    - Test 3 (CupsPrinterDriver): with subprocess.run patched at claude_teletype.printing.drivers.subprocess.run, call write_bytes(b"\x1b[T\x04\x00\x00\x00\x01\xb5") then write("\n") to flush. Assert the `input=` bytes passed to subprocess.run contain 0xb5 verbatim (no 0x3f substitution).
    - Test 4 (composed round-trip): ProfilePrinterDriver(CupsPrinterDriver("q"), ppds profile) — write a cp437-encodable non-ASCII char plus "\n"; assert the flushed subprocess input contains the exact codepage_command and encoded text bytes, altered nowhere.
  </behavior>
  <action>
    Create tests/test_byte_integrity.py with the four tests above. These tests MUST fail against the current drivers.py (RED) — run them and confirm failure before Task 2. Use `patch("claude_teletype.printing.drivers.subprocess.run")` (canonical module path per project convention). Do not import from teletype.py — this file is the driver-layer contract lock only. Commit the failing tests as `test(31-01): add failing byte-integrity regression tests`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_byte_integrity.py -q; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_byte_integrity.py exists with >= 4 test functions
    - `grep -c '\\\\xb5' tests/test_byte_integrity.py` >= 2 (the sentinel byte appears in assertions)
    - `uv run pytest tests/test_byte_integrity.py -q` FAILS against unmodified drivers.py (RED confirmed)
    - subprocess.run is patched via the string "claude_teletype.printing.drivers.subprocess.run"
  </acceptance_criteria>
  <done>Regression test committed and demonstrably RED against current code.</done>
</task>

<task type="auto">
  <name>Task 2: Fix _send_raw (CR-01) and CupsPrinterDriver byte buffer (CR-02)</name>
  <files>src/claude_teletype/printing/drivers.py</files>
  <read_first>
    - .planning/v1.6-REVIEW.md (CR-01 and CR-02 fix guidance)
    - src/claude_teletype/printing/drivers.py (full file — _send_raw callers: _ensure_init, write newline branch, _ensure_codepage, write_bytes, end_response, close)
  </read_first>
  <action>
    Two changes, per BYTE-01/BYTE-02 and the review's prescribed fixes:

    1. `ProfilePrinterDriver._send_raw`: replace the `data.decode("ascii", errors="replace")` + `self._inner.write(...)` body with `self._inner.write_bytes(data)` (guarded by `if data:` as today). Keep the docstring's atomicity rationale; update it to state delivery is via the inner write_bytes channel. Note: the CR+LF+reinit newline_data composed in `write("\n")` still flows through _send_raw as ONE call — the atomic-transfer constraint is preserved (write_bytes is a single transfer on every inner driver).

    2. `CupsPrinterDriver`: convert `_line_buffer` from `list[str]` to `list[bytes]`. In `write()`, append `char.encode("ascii", errors="replace")` and flush when `char == "\n"` (unchanged trigger). In `write_bytes()`, append `data` raw and flush when `b"\n" in data`. In `_flush_line()`, pass `b"".join(self._line_buffer)` directly as `input=` to subprocess.run — no decode/encode round trip anywhere. `flush()` and `close()` keep their existing buffer-nonempty guards.

    Do NOT touch: the write('\n') external newline contract, chunk_writes, UsbPrinterDriver, FilePrinterDriver, JukiPrinterDriver alias.
  </action>
  <verify>
    <automated>uv run pytest tests/test_byte_integrity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'decode("ascii"' src/claude_teletype/printing/drivers.py` returns no hits inside _send_raw or CupsPrinterDriver (FilePrinterDriver/UsbPrinterDriver write() encode paths for str input are unchanged and allowed)
    - `grep -c '_inner.write_bytes(data)' src/claude_teletype/printing/drivers.py` >= 1 within _send_raw
    - CupsPrinterDriver._line_buffer type annotation is `list[bytes]`
    - `uv run pytest tests/test_byte_integrity.py -q` exits 0 (GREEN)
  </acceptance_criteria>
  <done>All four regression tests pass; commit `feat(31-01): route raw bytes through write_bytes in _send_raw and CupsPrinterDriver`.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate existing test_printer.py helpers and assertions to the write_bytes path</name>
  <files>tests/test_printer.py</files>
  <read_first>
    - tests/test_printer.py (helper `_collect_raw` at ~line 512 — collects only inner.write calls; ProfilePrinterDriver test classes; CupsPrinterDriver tests ~lines 133-215 and end_response flush tests ~lines 778-830)
    - src/claude_teletype/printing/drivers.py (post-Task-2 state)
  </read_first>
  <action>
    Raw sequences now arrive at mock inner drivers via write_bytes, so tests that reconstruct output from `inner.write.call_args_list` alone will miss init/reset/ESC bytes. Update `_collect_raw` (and any sibling collectors) to iterate `inner.mock_calls` IN ORDER, appending `args[0].encode("ascii")` for `write` calls and raw `args[0]` for `write_bytes` calls — preserving interleaving so `raw.startswith(expected_init)` style assertions stay meaningful. Update CupsPrinterDriver tests whose expectations encode the old str-buffer behavior (e.g. subprocess input assertions built from str joins) to expect the byte-joined payload. Behavior note: multi-char strings written via inner.write to CupsPrinterDriver previously did NOT flush on embedded "\n" (the `char == "\n"` check); raw newline bytes via write_bytes now DO flush (`b"\n" in data`) — adjust any test relying on the old non-flush quirk and keep `test_write_bytes_does_not_handle_newlines_specially` semantics for ProfilePrinterDriver (write_bytes(b"\n") passes verbatim, no CR+LF injection).
  </action>
  <verify>
    <automated>uv run pytest tests/test_printer.py tests/test_byte_integrity.py tests/test_codepage.py tests/test_output.py tests/test_markdown.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `_collect_raw` in tests/test_printer.py references both `write` and `write_bytes` mock calls in call order
    - `uv run pytest tests/test_printer.py -q` exits 0
    - `uv run pytest tests/test_codepage.py tests/test_output.py tests/test_markdown.py -q` exits 0 (downstream consumers of the driver contract unaffected)
    - No test was deleted to pass — count of test functions in tests/test_printer.py is >= the pre-change count
  </acceptance_criteria>
  <done>Driver-layer and consumer test files green; commit `test(31-01): migrate driver test helpers to write/write_bytes interleaved collection`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → `lp` subprocess | printer payload bytes passed as stdin to a fixed argv (no shell) |
| custom TOML profiles → driver | user-authored byte sequences flow to hardware verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | CupsPrinterDriver._flush_line | mitigate | argv stays a fixed list (`["lp","-o","raw","-d",name]`), payload only via `input=` bytes — no shell interpolation introduced by the fix |
| T-31-02 | Tampering | ProfilePrinterDriver._send_raw | accept | profile bytes are local user config destined for local hardware; delivering them verbatim is the feature, not an injection surface |
| T-31-SC | Tampering | package installs | accept | no new dependencies introduced in this phase |
</threat_model>

<verification>
- `uv run pytest tests/test_byte_integrity.py -q` — all 4 regression tests green
- `uv run pytest tests/test_printer.py tests/test_codepage.py tests/test_output.py tests/test_markdown.py -q` — driver contract consumers green
- `grep -n 'decode' src/claude_teletype/printing/drivers.py` — no decode inside _send_raw or CupsPrinterDriver
</verification>

<success_criteria>
- BYTE-01: _send_raw delivers via inner.write_bytes; ppds 0xb5 survives to the inner driver (regression Test 1/2 green)
- BYTE-02: CupsPrinterDriver buffers bytes and hands `b"".join(...)` to lp; 0xb5 survives to subprocess input (regression Test 3/4 green)
- BYTE-03: tests/test_byte_integrity.py exists, was RED before the fix, is GREEN after, and fails if any byte is altered
</success_criteria>

<output>
Create `.planning/phases/31-byte-integrity-criticals/31-01-SUMMARY.md` when done
</output>
