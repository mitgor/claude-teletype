---
phase: 31-byte-integrity-criticals
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/claude_teletype/teletype.py
  - tests/test_teletype.py
autonomous: true
requirements: [BYTE-04]
must_haves:
  truths:
    - "Typewriter mode with a high-byte custom profile starts without UnicodeDecodeError — init bytes go out via write_bytes"
    - "The profile reset_sequence leaves as ONE write_bytes call, never per-byte chr() writes"
    - "Ctrl-C (KeyboardInterrupt) exits cleanly through restore/formfeed/reset with no traceback"
  artifacts:
    - path: "src/claude_teletype/teletype.py"
      provides: "byte-clean init/reset delivery and KeyboardInterrupt-safe read loop"
      contains: "write_bytes"
  key_links:
    - from: "src/claude_teletype/teletype.py"
      to: "PrinterDriver.write_bytes"
      via: "init and reset delivery"
      pattern: "driver\\.write_bytes\\("
---

<objective>
Fix the three WR-02 defects in typewriter mode: strict-ASCII init decode, per-byte fragmented reset writes, and the uncaught KeyboardInterrupt on Ctrl-C. Close with the full 905-test suite as the phase gate.

Purpose: A custom TOML profile with high bytes in init/line-spacing/pitch crashes teletype mode before printing anything; the reset sequence is fragmented into single-byte transfers (the exact CH341 failure mode the module docstring forbids); and Ctrl-C produces a traceback instead of the documented clean exit.

Output: Corrected `teletype.py`, updated `tests/test_teletype.py` including a KeyboardInterrupt clean-exit test, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v1.6-REVIEW.md
@src/claude_teletype/teletype.py
</context>

<interfaces>
From src/claude_teletype/printing/drivers.py (fixed in plan 31-01):

```python
class PrinterDriver(Protocol):
    def write(self, char: str) -> None: ...       # text channel
    def write_bytes(self, data: bytes) -> None: ...  # raw channel, single transfer, byte-verbatim
```

From src/claude_teletype/teletype.py (the defects, per WR-02):
- line 45: `driver.write(init_data.decode("ascii"))` — strict decode, crashes on bytes >= 0x80
- lines 76-78: `for b in profile.reset_sequence: driver.write(chr(b))` — fragmented per-byte transfers
- read loop: `tty.setcbreak` leaves ISIG on → Ctrl-C raises KeyboardInterrupt at `sys.stdin.read(1)`, propagates uncaught
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix teletype init/reset byte delivery and Ctrl-C handling (WR-02)</name>
  <files>src/claude_teletype/teletype.py, tests/test_teletype.py</files>
  <read_first>
    - .planning/v1.6-REVIEW.md (section WR-02 — the three defects and prescribed fixes)
    - src/claude_teletype/teletype.py (full file, 79 lines)
    - tests/test_teletype.py (helper `_written_bytes` at ~line 15 — collects only driver.write calls; test_juki_sends_init_codes, test_juki_enter_sends_cr_lf, test_ctrl_c_exits_cleanly, test_formfeed_on_exit, test_terminal_restored_* patterns)
  </read_first>
  <behavior>
    - Test: init sequence containing a byte >= 0x80 (build a profile via dataclasses.replace(get_profile("juki"), init_sequence=b"\x1b[T\x04\x00\x00\x00\x01\xb5")) reaches the driver verbatim as one write_bytes call — no UnicodeDecodeError, trailing byte is 0xb5.
    - Test: reset_sequence goes out as exactly ONE driver.write_bytes call with the full sequence (assert call count == 1 for the reset payload, not len(sequence) write calls).
    - Test: sys.stdin.read raising KeyboardInterrupt → run_teletype returns normally (no exception), and termios restore + formfeed + reset + driver.close() all still happen (the finally path).
    - Existing tests: `_written_bytes` updated to interleave driver.write (encoded) and driver.write_bytes (raw) in mock call order, so ordering assertions like `raw.startswith(expected_init)` and `after_init == b"\r\n\f..."` remain valid.
  </behavior>
  <action>
    Three source changes in run_teletype, per WR-02's prescribed fix (D-source: v1.6-REVIEW.md):

    1. Init: replace `driver.write(init_data.decode("ascii"))` with `driver.write_bytes(init_data)` — single transfer, byte-verbatim, no decode.
    2. Reset: replace the `for b in profile.reset_sequence: driver.write(chr(b))` loop in the finally block with `driver.write_bytes(profile.reset_sequence)` — one atomic transfer.
    3. Ctrl-C: wrap the `while True:` read loop (inside the existing try, after tty.setcbreak) in `try: ... except KeyboardInterrupt: pass` — the existing finally already performs restore/formfeed/reset/close. Keep the `ch == "\x03"` break check (harmless if cbreak ever delivers it raw).

    Do NOT change: the `use_crlf` newline strategy (`driver.write("\r\n")` / `driver.write("\n")` stays on the text channel per the MD-08 boundary), formfeed via `driver.write("\f")`, stderr echo behavior, or the termios restore ordering.

    Test changes in tests/test_teletype.py: update `_written_bytes` to iterate `driver.mock_calls` in order (write → `.encode("ascii")`, write_bytes → raw bytes) and add the three behavior tests above. Keep the existing patch targets (`claude_teletype.teletype.termios/tty/sys`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_teletype.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'driver.write_bytes(' src/claude_teletype/teletype.py` == 2 (init + reset)
    - `grep -c 'chr(b)' src/claude_teletype/teletype.py` == 0
    - `grep -c 'decode("ascii")' src/claude_teletype/teletype.py` == 0
    - `grep -c 'KeyboardInterrupt' src/claude_teletype/teletype.py` >= 1
    - `uv run pytest tests/test_teletype.py -q` exits 0, including a test where stdin.read raises KeyboardInterrupt and asserts termios.tcsetattr + driver.close were called
    - No existing test deleted; test function count in tests/test_teletype.py >= pre-change count + 3
  </acceptance_criteria>
  <done>Typewriter mode delivers init/reset via single write_bytes transfers, survives non-ASCII profile bytes, and exits cleanly on Ctrl-C; commits: `test(31-02): add failing teletype byte/Ctrl-C tests` then `feat(31-02): write_bytes init/reset and KeyboardInterrupt-safe teletype loop`.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full test suite green</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 31 success criteria — all four must now hold)
  </read_first>
  <action>
    Run the complete test suite (905+ tests: the pre-existing 905 plus the new byte-integrity and teletype tests from this phase). Any failure anywhere — including files this phase never touched — must be fixed or root-caused before the phase closes; the byte-channel change in 31-01 touches a contract many modules consume (markdown renderer style channel, chunk_writes, end_response cut flushing). Do not skip, deselect, or xfail tests to get green.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exits 0 with zero failures and zero errors
    - Total collected test count >= 905
    - `uv run pytest tests/test_byte_integrity.py tests/test_teletype.py tests/test_printer.py -q` exits 0 (phase-specific re-confirmation)
  </acceptance_criteria>
  <done>Full suite green; phase success criteria 1-4 all verifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin (terminal) → printer | raw keystrokes forwarded to local hardware in cbreak mode |
| custom TOML profile → driver | user-authored init/reset bytes sent verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | DoS | run_teletype finally block | mitigate | KeyboardInterrupt caught inside the loop only; finally still restores termios settings so the terminal is never left in cbreak mode |
| T-31-04 | Tampering | init/reset delivery | accept | profile bytes are local trusted config for local hardware; verbatim delivery is the requirement |
| T-31-SC | Tampering | package installs | accept | no new dependencies in this phase |
</threat_model>

<verification>
- `uv run pytest tests/test_teletype.py -q` — WR-02 fixes green including Ctrl-C clean-exit test
- `uv run pytest -q` — full 905+ suite green (phase gate)
</verification>

<success_criteria>
- BYTE-04: non-ASCII init bytes cause no UnicodeDecodeError; reset_sequence is one write_bytes transfer; Ctrl-C exits through restore/formfeed/reset with no traceback
- Full test suite (>= 905 tests) passes with zero failures
</success_criteria>

<output>
Create `.planning/phases/31-byte-integrity-criticals/31-02-SUMMARY.md` when done
</output>
