---
phase: 34-architecture-cleanup
plan: 04
type: execute
wave: 3
depends_on: [34-01, 34-02]
files_modified:
  - src/claude_teletype/printing/__init__.py
  - src/claude_teletype/screens/__init__.py
  - src/claude_teletype/printing/drivers.py
  - src/claude_teletype/printing/selection.py
  - src/claude_teletype/cli.py
  - src/claude_teletype/config.py
  - tests/test_printer.py
  - tests/test_config.py
  - tests/test_cli.py
  - tests/test_teletype.py
autonomous: true
requirements: [ARCH-CLEAN-03]
must_haves:
  truths:
    - "The 'juki' alias profile is the ONLY juki backward-compat path — JukiPrinterDriver, discover_printer(juki=), --juki, and config.juki are gone"
    - "printing/__init__.py is a thin namespace docstring like rendering/ and screens/ — no unused 28-name re-export facade"
    - "No docstring anywhere claims shim modules exist (they were deleted before v1.7)"
    - "A stale `juki = true` key in an existing config TOML is ignored gracefully, not a crash"
  artifacts:
    - path: "src/claude_teletype/printing/__init__.py"
      provides: "docstring-only namespace init"
      max_lines: 15
    - path: "src/claude_teletype/screens/__init__.py"
      provides: "docstring without the false '*_screen.py re-export shims remain' claim"
  key_links:
    - from: "tests/test_printer.py"
      to: "src/claude_teletype/printing/profiles.py"
      via: "juki byte pins re-anchored on BUILTIN_PROFILES['juki-6100'] fields instead of JukiPrinterDriver class constants"
      pattern: "juki-6100"
---

<objective>
Delete shim-era dead code (ARCH-07, ARCH-08): trim the zero-consumer 91-line printing/__init__ facade, fix both stale shim docstrings, and remove the three redundant juki compat paths (JukiPrinterDriver subclass, discover_printer's juki= parameter, config.juki + --juki + honor_config_juki plumbing) — the "juki" alias profile alone carries backward compat. Verify the dead all_profiles parameter on _render_markdown_to_driver is already gone (Phase 33 dropped it).

Decision (facade): TRIM, not adopt. The review proved zero consumers (`from claude_teletype.printing import X` appears nowhere in src or tests; all 20+ importers use submodule paths), and rendering/__init__.py + screens/__init__.py already set the docstring-only pattern. Adopting would create a 28-name surface nobody asked for and a second place to forget on every submodule change.

Output: leaner printing package, single juki compat path, corrected docstrings, full suite green with juki byte pins re-anchored (not deleted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.6-ARCHITECTURE-REVIEW.md
@src/claude_teletype/printing/__init__.py
@src/claude_teletype/screens/__init__.py

<interfaces>
Deletion targets (verified line refs, pre-34-02 numbering — re-locate by symbol):
- drivers.py ~402-416: `class JukiPrinterDriver(ProfilePrinterDriver)` with RESET/LINE_SPACING/FIXED_PITCH class constants — exercised only by tests/test_printer.py:508-627 and re-exported by the facade being trimmed.
- selection.py: `juki: bool = False` parameter on discover_printer, its `if juki and profile is None: profile = get_profile("juki")` branch, and the deprecation note in the docstring. get_profile import may become unused — remove if so.
- cli.py: `juki_flag`/`honor_config_juki` parameters on _resolve_profile_selection (~317-318, 334-336) and their two branches (~371-374, ~382-384); the `--juki` typer option (~704-707); the `juki=juki, honor_config_juki=True` call-site args (~763-767).
- config.py: `juki: bool = False` field (~99); `"juki"` in the bool-coercion tuple (~180); the profile-name comment at ~41 mentions "juki" as a profile name — that stays (the alias profile lives).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim printing/__init__ facade; fix both stale shim docstrings</name>
  <files>src/claude_teletype/printing/__init__.py, src/claude_teletype/screens/__init__.py</files>
  <read_first>src/claude_teletype/printing/__init__.py (all 91 lines); src/claude_teletype/screens/__init__.py; v1.6-ARCHITECTURE-REVIEW.md ARCH-07; for the target shape, src/claude_teletype/rendering/__init__.py</read_first>
  <action>
    1. Replace printing/__init__.py entirely with a docstring-only module (match the rendering/screens pattern): name the package's submodules (drivers, discovery, selection, detection, profiles, registry, catalog, pipeline) and state that callers import submodules directly — canonical patch targets are `claude_teletype.printing.{discovery,selection,detection,drivers}` (the locked test convention). No imports, no __all__. The docstring must NOT claim any shim modules exist.
    2. screens/__init__.py: delete the false sentence "top-level `*_screen.py` modules remain as re-export shims for the pre-split import paths" — the shims were deleted. Keep the rest of the docstring.
    3. Guard: run `grep -rn "from claude_teletype.printing import " src tests` first; every hit must be a submodule import (e.g. `import discovery as _discovery`). If any hit imports a NAME the facade re-exported, stop and repoint that consumer to its submodule path in the same commit — the review found zero, but verify, don't trust.
  </action>
  <verify>
    <automated>python3 -c "import claude_teletype.printing, claude_teletype.cli, claude_teletype.tui, claude_teletype.diagnose" && python3 -m pytest tests -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l < src/claude_teletype/printing/__init__.py` <= 15
    - `grep -c 'shims remain' src/claude_teletype/printing/__init__.py src/claude_teletype/screens/__init__.py` == 0 in both
    - `grep -c '__all__' src/claude_teletype/printing/__init__.py` == 0
  </acceptance_criteria>
  <done>Facade trimmed with justification recorded; no docstring references deleted shims; all imports still load clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove juki compat paths (2)-(4); re-pin tests against the profile catalog</name>
  <files>src/claude_teletype/printing/drivers.py, src/claude_teletype/printing/selection.py, src/claude_teletype/cli.py, src/claude_teletype/config.py, tests/test_printer.py, tests/test_config.py, tests/test_cli.py</files>
  <read_first>drivers.py JukiPrinterDriver class; selection.py discover_printer; cli.py _resolve_profile_selection + main() option list; config.py Config dataclass and its TOML loading path (confirm how unknown TOML keys are handled before deleting the field); tests/test_printer.py:508-627 and 1044+ (the tests being re-pinned); v1.6-ARCHITECTURE-REVIEW.md ARCH-08</read_first>
  <action>
    1. drivers.py: delete the JukiPrinterDriver class (and the now-unused `get_profile` import if nothing else in drivers.py uses it). LOCKED CONSTRAINT: this deletion is the ONLY drivers.py change — zero edits to ProfilePrinterDriver or any byte-path code (Phase 31 locks: write_bytes channel, atomic CR+LF).
    2. selection.py: remove the `juki` parameter and its compat branch from discover_printer; update the docstring (profile= is the only selector). Remove the get_profile import if now unused.
    3. cli.py: remove `juki_flag`/`honor_config_juki` parameters and both branches from _resolve_profile_selection (precedence becomes: --printer > config.printer_profile > auto-detect > generic — update the docstring precedence line); remove the `--juki` typer option from main()'s signature and the call-site args. `registry.get("juki")` must appear nowhere in cli.py afterward; a user typing `--printer juki` still works via the alias profile.
    4. config.py: delete the `juki` field and drop `"juki"` from the bool-coercion tuple at ~180. CRITICAL: existing user configs on disk may contain `juki = true` — confirm the TOML→Config path ignores unknown keys (it filters by dataclass fields or similar); if it would raise on an unknown key, add the ignore explicitly. Add a regression test: load a TOML containing `juki = true` → no exception, field absent.
    5. Tests — RE-PIN, do not delete (keeps collected count >= 965):
       - tests/test_printer.py:508-627: convert each JukiPrinterDriver test to construct `ProfilePrinterDriver(inner, BUILTIN_PROFILES["juki-6100"])` and assert against the profile's fields (`init_sequence`, `line_spacing`, `char_pitch`, `reinit_sequence`) instead of the deleted RESET/LINE_SPACING/FIXED_PITCH class constants. The byte expectations themselves are unchanged — they now pin the catalog, which is where the contract lives.
       - tests/test_printer.py:1044+ (`discover_printer(juki=True)` tests): convert to `discover_printer(profile=get_profile("juki"))` asserting the identical wrapping behavior.
       - tests/test_cli.py: remove/convert any --juki or config.juki=true cases; add one test that `--printer juki` resolves to the juki-6100-equivalent profile via the alias (the load-bearing compat, now the only one).
       - tests/test_teletype.py: convert test_cli_teletype_passes_juki_profile (~lines 471-487, invokes `["--teletype", "--juki"]`) to `["--teletype", "--printer", "juki"]` — same mock_run_teletype assertion; it exercises the surviving alias path.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_printer.py tests/test_cli.py tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'JukiPrinterDriver' src tests` returns nothing
    - `grep -v '^\s*#' src/claude_teletype/printing/selection.py | grep -c 'juki'` == 0
    - `grep -c 'honor_config_juki\|juki_flag\|--juki' src/claude_teletype/cli.py` == 0
    - `grep -c 'juki: bool' src/claude_teletype/config.py` == 0; stale-TOML-key regression test passes
    - `grep -rn 'all_profiles' src/claude_teletype/cli.py` == empty (verification that Phase 33 + plan 34-02 left no residue — the dead _render_markdown_to_driver parameter must remain absent)
    - Alias intact: `python3 -c "from claude_teletype.printing.profiles import get_profile; p = get_profile('juki'); assert p.init_sequence == b'\x1b\x1aI'"` succeeds
    - Full suite: `python3 -m pytest tests -q` — 0 failures, >= 965 collected
  </acceptance_criteria>
  <done>Exactly one juki compat path remains (the alias profile); byte contracts re-pinned against the catalog; stale configs load safely; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user TOML config → Config dataclass | stale `juki = true` keys in existing configs cross into the loader after the field is deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | DoS | config loader crash on stale key | mitigate | explicit unknown-key tolerance + regression test (Task 2 step 4) |
| T-34-07 | Tampering | juki byte-contract loss during test re-pin | mitigate | identical byte expectations re-anchored on BUILTIN_PROFILES["juki-6100"]; alias assertion in acceptance criteria |
</threat_model>

<verification>
- `python3 -m pytest tests -q` — full suite green, >= 965 collected
- `grep -rn 'JukiPrinterDriver\|honor_config_juki' src tests` == empty
- `wc -l < src/claude_teletype/printing/__init__.py` <= 15
</verification>

<success_criteria>
Success criterion 3 (ARCH-07, ARCH-08) closed: facade trimmed with justification, stale docstrings fixed, juki compat reduced to the alias profile, dead parameter verified absent, full suite green.
</success_criteria>

<output>
Create `.planning/phases/34-architecture-cleanup/34-04-SUMMARY.md` when done.
</output>
