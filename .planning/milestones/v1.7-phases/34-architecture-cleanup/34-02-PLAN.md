---
phase: 34-architecture-cleanup
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - src/claude_teletype/cli.py
  - src/claude_teletype/tui.py
  - src/claude_teletype/screens/printer_setup.py
  - src/claude_teletype/printing/drivers.py
  - src/claude_teletype/printing/selection.py
  - tests/test_tui.py
  - tests/test_printer.py
  - tests/test_printer_setup_screen.py
  - tests/test_smart_startup.py
  - tests/test_tui_file_picker_keybinding.py
autonomous: true
requirements: [ARCH-CLEAN-01, ARCH-CLEAN-04]
must_haves:
  truths:
    - "The ProfileRegistry object flows cli → TeletypeApp → PrinterSetupScreen → create_driver_for_selection with no flatten-to-dict-and-rebuild"
    - "Fallback/unknown-profile diagnostics are visible inside the running TUI via notify(), not swallowed stderr prints (WR-04)"
    - "tui.py names the connection type through a public driver property — no _inner private reach-in (ARCH-06)"
    - "An unresolvable saved profile forces the status-bar profile to match the actual (unwrapped) driver (32-review IN-03)"
  artifacts:
    - path: "src/claude_teletype/printing/drivers.py"
      provides: "public read-only ProfilePrinterDriver.inner property"
      contains: "def inner"
    - path: "src/claude_teletype/screens/printer_setup.py"
      provides: "registry accepted directly; second-registry rebuild deleted"
    - path: "src/claude_teletype/tui.py"
      provides: "registry-based profile lookups and notify()-surfaced selection diagnostics"
  key_links:
    - from: "src/claude_teletype/cli.py"
      to: "src/claude_teletype/tui.py"
      via: "TeletypeApp(registry=registry, ...)"
      pattern: "registry=registry"
    - from: "src/claude_teletype/tui.py"
      to: "src/claude_teletype/printing/selection.py"
      via: "create_driver_for_selection(..., registry=..., diagnostics=...)"
      pattern: "diagnostics="
    - from: "src/claude_teletype/tui.py"
      to: "src/claude_teletype/printing/drivers.py"
      via: "driver.inner public property"
      pattern: "\\.inner"
---

<objective>
Thread the ProfileRegistry object end-to-end (cli → TeletypeApp → PrinterSetupScreen → create_driver_for_selection), deleting the flatten-to-dict and the second-registry rebuild (ARCH-02). Surface selection diagnostics in the TUI via notify() (WR-04). Replace the `_inner` private reach-in with a public property (ARCH-06). Fix the 32-review IN-01 enabled-queue recommendation gate and IN-03 status-bar divergence while touching those exact lines.

Purpose: one profile currency with one failure semantic across the whole hand-off chain; diagnostics the user can actually see under Textual.
Output: registry-threaded cli/tui/setup-screen; `all_profiles` dict plumbing deleted everywhere; public `inner` property.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.6-ARCHITECTURE-REVIEW.md
@.planning/phases/32-setup-detection-flow-fixes/32-REVIEW.md
@src/claude_teletype/printing/selection.py
@src/claude_teletype/printing/registry.py

<interfaces>
From src/claude_teletype/printing/selection.py after plan 34-01:

```python
def create_driver_for_selection(
    selection: PrinterSelection,
    discovery: DiscoveryResult,
    all_profiles: dict[str, PrinterProfile] | None = None,  # shim — DELETED by this plan
    *,
    registry: ProfileRegistry | None = None,
    diagnostics: list[str] | None = None,
) -> PrinterDriver
```

From src/claude_teletype/printing/registry.py:

```python
registry.get(name)      # case-insensitive; raises ValueError with available names
registry.names()        # list[str], case-preserved
registry.match_vidpid(vid, pid)
```

Current dict-lookup sites to replace:
- cli.py:772 `all_profiles = registry.all()`; cli.py:920 `all_profiles=all_profiles`; cli.py:925-926 `in all_profiles` membership + subscript; cli.py:957 `all_profiles=all_profiles` (TeletypeApp kwarg); cli.py:398/423 `_resolve_print_context` returns `registry.all()`
- tui.py:131 `all_profiles: dict | None`; :162 `self._all_profiles`; :245 (PrinterSetupScreen kwarg); :267 (create_driver_for_selection kwarg); :476-478 (settings names list); :510, :634, :753, :1056 (`self._all_profiles.get(...)`)
- tui.py:361 `inner = driver._inner`
- printer_setup.py:104-119 (constructor rebuilds a second ProfileRegistry from the dict); :138-141 (Select options); :307 (`suggested_profile in self._all_profiles`)
- printer_setup.py:~282 `if kernel_owns and self._discovery.cups_printers:` (IN-01)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Public `inner` property on ProfilePrinterDriver; tui.py uses it (ARCH-06)</name>
  <files>src/claude_teletype/printing/drivers.py, src/claude_teletype/tui.py, tests/test_printer.py</files>
  <read_first>src/claude_teletype/printing/drivers.py lines 225-280 (ProfilePrinterDriver __init__ and is_connected property — place the new property beside is_connected); src/claude_teletype/tui.py lines 344-375 (_printer_info); v1.6-ARCHITECTURE-REVIEW.md ARCH-06</read_first>
  <behavior>
    - Test 1: `ProfilePrinterDriver(inner_stub, profile).inner is inner_stub` (read-only property, returns the wrapped driver).
    - Test 2: assigning to `.inner` raises AttributeError (property has no setter).
  </behavior>
  <action>
    In drivers.py, add to ProfilePrinterDriver, adjacent to the existing `is_connected` property:

    ```
    @property
    def inner(self) -> PrinterDriver:
        """The wrapped transport driver (read-only; ARCH-06 public surface)."""
        return self._inner
    ```

    LOCKED CONSTRAINT: this property is the ONLY drivers.py change in this task — do not touch write/write_bytes/close or any byte-path code (Phase 31 locks).

    In tui.py `_printer_info` (line ~361), replace `inner = driver._inner` with `inner = driver.inner`. No other _printer_info changes.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_printer.py tests/test_tui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_inner' src/claude_teletype/tui.py` == 0
    - `grep -c 'def inner' src/claude_teletype/printing/drivers.py` == 1
    - drivers.py diff touches only the ProfilePrinterDriver class body (property addition) — no byte-path lines changed
  </acceptance_criteria>
  <done>tui.py has zero private reach-ins into ProfilePrinterDriver; the capability is a documented public property.</done>
</task>

<task type="auto">
  <name>Task 2: Thread registry through cli.py and tui.py; surface diagnostics via notify (WR-04, IN-03)</name>
  <files>src/claude_teletype/cli.py, src/claude_teletype/tui.py, tests/test_tui.py, tests/test_smart_startup.py</files>
  <read_first>src/claude_teletype/cli.py lines 393-423 (_resolve_print_context), 760-780 and 895-960 (main() smart-startup + TeletypeApp construction); src/claude_teletype/tui.py lines 125-170 (constructor), 238-285 (_show_setup_screen/_handle_setup_result), 470-520, 625-645, 745-760, 1045-1065 (dict lookup sites); 32-REVIEW.md WR-04 and IN-03</read_first>
  <action>
    cli.py:
    1. `_resolve_print_context`: return `(config, registry, resolved_profile)` instead of `(config, registry.all(), resolved_profile)`; update its docstring. Callers currently discard the middle value (`config, _, resolved_profile = ...`) — verify with grep and leave them as-is.
    2. main(): delete `all_profiles = registry.all()` (line ~772). At the smart-startup call (~920) pass `registry=registry` (drop `all_profiles=`). Replace lines ~925-926 (`if config.saved_printer_profile and config.saved_printer_profile in all_profiles: resolved_profile = all_profiles[...]`) with a registry resolution that also closes IN-03:

    ```
    if config.saved_printer_profile:
        try:
            resolved_profile = registry.get(config.saved_printer_profile)
        except ValueError:
            resolved_profile = None  # IN-03: status bar must not claim a profile the driver doesn't wear
    ```

    3. TeletypeApp construction (~957): replace `all_profiles=all_profiles` with `registry=registry`.

    tui.py:
    4. Constructor: replace `all_profiles: dict | None = None` with `registry: "ProfileRegistry | None" = None` (TYPE_CHECKING import or string annotation — do not add a module-top runtime import of registry if one does not already exist); store `self._registry = registry`. Delete `self._all_profiles`.
    5. Add a private helper on TeletypeApp:

    ```
    def _lookup_profile(self, name: str):
        """Case-insensitive registry lookup; None when no registry or unknown name."""
        if self._registry is None or not name:
            return None
        try:
            return self._registry.get(name)
        except ValueError:
            return None
    ```

    Replace all four `self._all_profiles.get(self._profile_name)`-style sites (~510, ~634, ~753, ~1056) with `self._lookup_profile(...)`. Site ~753 uses `result["profile"]` as the name.
    6. Settings screen call (~476-478): `available_profiles = sorted(self._registry.names()) if self._registry is not None else ["generic"]`.
    7. `_show_setup_screen` (~245): pass `registry=self._registry` to PrinterSetupScreen (Task 3 changes its signature; do both in the same commit or run Task 3 first — executor's choice, suite must be green at plan end, per-task ordering may interleave).
    8. `_handle_setup_result` (~267) — WR-04: capture and surface diagnostics:

    ```
    diags: list[str] = []
    driver = create_driver_for_selection(
        result, self._discovery, registry=self._registry, diagnostics=diags
    )
    for message in diags:
        self.notify(message, severity="warning", timeout=8)
    ```

    Tests: update every test constructing TeletypeApp(all_profiles=...) — including the four sites in tests/test_tui_file_picker_keybinding.py:206-269 (convert to registry=ProfileRegistry({...})) to pass `registry=ProfileRegistry({...})`; add one test asserting an unknown saved profile in _handle_setup_result produces a notify (patch/collect App.notify) — the WR-04 visibility contract.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_tui.py tests/test_smart_startup.py tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'all_profiles' src/claude_teletype/cli.py` == 0 and `grep -c 'all_profiles' src/claude_teletype/tui.py` == 0
    - `grep -c 'registry.all()' src/claude_teletype/cli.py` == 0
    - `grep -c '_lookup_profile' src/claude_teletype/tui.py` >= 5 (1 def + 4 sites)
    - A test proves setup-flow diagnostics reach self.notify (WR-04)
  </acceptance_criteria>
  <done>cli builds one registry and hands the object through; tui resolves every profile via the registry and shows selection diagnostics as Textual notifications.</done>
</task>

<task type="auto">
  <name>Task 3: PrinterSetupScreen accepts the registry directly; delete the rebuild; IN-01 gate; drop selection.py shim</name>
  <files>src/claude_teletype/screens/printer_setup.py, src/claude_teletype/printing/selection.py, tests/test_printer_setup_screen.py</files>
  <read_first>src/claude_teletype/screens/printer_setup.py lines 95-150 (constructor + compose Select), 270-315 (kernel-owns recommendation + classify suggestion); 32-REVIEW.md IN-01</read_first>
  <action>
    printer_setup.py:
    1. Constructor: `def __init__(self, discovery: DiscoveryResult, registry: ProfileRegistry | None = None, **kwargs)`. Delete `self._all_profiles` and the second-registry rebuild (lines ~114-119). Set `self._registry = registry if registry is not None else ProfileRegistry({"generic": PrinterProfile(name="generic")})` (import PrinterProfile from claude_teletype.printing.profiles at module top; ProfileRegistry import already exists at line 40). This default serves direct constructions in tests; cli/tui always pass the real registry.
    2. compose() Select (~138-141): `sorted_names = sorted(self._registry.names())`; default value `"generic" if "generic" in self._registry.names() else sorted_names[0]`.
    3. Suggestion check (~304-309): replace `classification.suggested_profile in self._all_profiles` with a try/except around `self._registry.get(classification.suggested_profile)` — apply the suggestion only when resolvable AND present in the Select's option values (case-preserved names), else "generic". Keep the BRIDGE/UNKNOWN → "generic" logic untouched.
    4. IN-01 (~282): change `if kernel_owns and self._discovery.cups_printers:` to `if kernel_owns and any(q.enabled for q in self._discovery.cups_printers):` — never recommend a connection method the Connect button will refuse.

    selection.py:
    5. Delete the transitional `all_profiles` parameter from create_driver_for_selection (the 34-01 shim): signature becomes `(selection, discovery, *, registry=None, diagnostics=None)`. Remove the dict→registry conversion branch; the internal default stays `ProfileRegistry(BUILTIN_PROFILES)`. Update the docstring (registry is the only catalog input — ARCH-02 closed).

    Tests: update PrinterSetupScreen constructions to pass `registry=`; add a test that the kernel-owns recommendation is NOT applied when all CUPS queues are disabled (IN-01); update any remaining create_driver_for_selection callers using all_profiles=.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_printer_setup_screen.py tests/test_smart_startup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'all_profiles' src/claude_teletype/screens/printer_setup.py` == 0 and `grep -c 'all_profiles' src/claude_teletype/printing/selection.py` == 0
    - `grep -rn 'all_profiles' src/claude_teletype/` returns nothing (the dict currency is extinct in src)
    - `grep -c 'ProfileRegistry(' src/claude_teletype/screens/printer_setup.py` == 1 (the test-only default; no rebuild from a flattened dict)
    - IN-01 test passes; full suite: `python3 -m pytest tests -q` — 0 failures, >= 965 collected
  </acceptance_criteria>
  <done>PrinterSetupScreen consumes the registry object directly; the flatten-and-rebuild round trip is gone; the deprecated dict parameter no longer exists anywhere in src.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved TOML config → profile resolution | stale/hand-edited saved_printer_profile names cross into driver creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Repudiation | TUI setup-flow fallback (WR-04) | mitigate | diagnostics surfaced via self.notify — user sees degradation in the running app |
| T-34-04 | Elevation | wrong-profile driver from stale config (IN-03) | mitigate | registry.get with explicit None on failure; status bar matches actual driver |
</threat_model>

<verification>
- `python3 -m pytest tests -q` — full suite green, >= 965 collected
- `grep -rn 'all_profiles' src/claude_teletype/` == empty; `grep -c '_inner' src/claude_teletype/tui.py` == 0
</verification>

<success_criteria>
Success criterion 1 (ARCH-02 + WR-01/02/04) and criterion 4 (ARCH-06) fully closed: registry object end-to-end, loud visible failures, no private reach-in, full suite green.
</success_criteria>

<output>
Create `.planning/phases/34-architecture-cleanup/34-02-SUMMARY.md` when done.
</output>
