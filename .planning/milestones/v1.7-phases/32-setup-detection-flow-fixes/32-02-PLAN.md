---
phase: 32-setup-detection-flow-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/claude_teletype/printing/registry.py
  - src/claude_teletype/printing/selection.py
  - src/claude_teletype/printing/discovery.py
  - src/claude_teletype/cli.py
  - tests/test_registry.py
  - tests/test_smart_startup.py
autonomous: true
requirements: [FLOW-02, FLOW-04, FLOW-01]

must_haves:
  truths:
    - "registry.get('MyPrinter') and registry.get('myprinter') both resolve an uppercase-named custom TOML profile; names() still returns the case-preserved key"
    - "--printer MyPrinter resolves (via _resolve_profile_selection → registry.get) instead of raising ValueError"
    - "match_saved_printer accepts profile_name and stamps it into both returned PrinterSelection variants; the caller-side mutation at cli.py:989 is deleted"
    - "PrinterSelection is frozen — attribute assignment raises FrozenInstanceError (only mutation site was cli.py:989, verified by grep)"
    - "create_driver_for_selection with connection_type='cups' and no cups_printer_name picks the first enabled discovered queue instead of silently returning NullPrinterDriver"
  artifacts:
    - path: "src/claude_teletype/printing/registry.py"
      provides: "Case-insensitive get() over case-preserved keys via a lowered-key index"
      contains: "lower"
    - path: "src/claude_teletype/printing/selection.py"
      provides: "match_saved_printer(profile_name=...) + cups-no-name defensive fallback"
      contains: "profile_name"
    - path: "src/claude_teletype/printing/discovery.py"
      provides: "frozen PrinterSelection"
      contains: "frozen=True"
  key_links:
    - from: "src/claude_teletype/cli.py"
      to: "match_saved_printer"
      via: "profile_name=config.saved_printer_profile or \"generic\" keyword argument"
      pattern: "profile_name=config.saved_printer_profile"
    - from: "src/claude_teletype/printing/registry.py"
      to: "self._profiles"
      via: "lowered-key index resolving to the case-preserved key"
      pattern: "\\.lower\\(\\)"
---

<objective>
Fix WR-03 (ProfileRegistry.get lowercases the lookup key against case-preserved
keys — uppercase custom TOML profiles are listed but unreachable) and ARCH-04
(smart-startup profile hand-off relies on cli.py mutating the dataclass
match_saved_printer returned; any future caller that forgets the mutation
silently reconnects with the generic profile). Also land the CR-03 defensive
half in create_driver_for_selection (cups selection with no queue name must not
silently become the simulator).

Purpose: FLOW-02, FLOW-04, and the driver-factory half of FLOW-01.
Output: Patched registry.py, selection.py, discovery.py, cli.py with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.6-REVIEW.md                    <!-- WR-03 (lines ~100-104) source of truth -->
@.planning/v1.6-ARCHITECTURE-REVIEW.md       <!-- ARCH-04 (lines ~49-53) source of truth -->
@src/claude_teletype/printing/registry.py
@src/claude_teletype/printing/selection.py
@src/claude_teletype/printing/discovery.py
@src/claude_teletype/cli.py
</context>

<interfaces>
From src/claude_teletype/printing/registry.py (fix site):
```python
def get(self, name: str) -> PrinterProfile:
    key = name.lower().strip()          # WR-03: lowercased key ...
    if key not in self._profiles:       # ... against case-preserved dict
```

From src/claude_teletype/printing/selection.py (fix sites):
```python
def match_saved_printer(saved_type: str, saved_id: str, discovery: DiscoveryResult) -> PrinterSelection | None:
    # both returns hardcode profile_name="generic" (lines 82, 91)
```
`create_driver_for_selection` cups branch (lines 156-158): `if selection.cups_printer_name:` — no-name falls through to NullPrinterDriver.

From src/claude_teletype/cli.py:982-989 (fix site):
```python
saved_match = match_saved_printer(
    config.saved_printer_type,
    config.saved_printer_id,
    discovery,
)
if saved_match is not None:
    saved_match.profile_name = config.saved_printer_profile or "generic"   # DELETE (ARCH-04)
```

From src/claude_teletype/printing/discovery.py:81-88: `@dataclass class PrinterSelection` — to be frozen. Grep confirms cli.py:989 is the only assignment to a PrinterSelection field anywhere in src/ and tests/.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-03 — case-insensitive ProfileRegistry.get over case-preserved keys</name>
  <files>src/claude_teletype/printing/registry.py, tests/test_registry.py</files>
  <read_first>
    - .planning/v1.6-REVIEW.md section "WR-03" (source of truth)
    - src/claude_teletype/printing/registry.py (full file — __init__ merge, _build_index, get, names)
    - tests/test_registry.py (existing assertion style; index-policy tests must keep passing)
  </read_first>
  <behavior>
    - Test 1: registry constructed with custom {"MyPrinter": profile} → get("MyPrinter"), get("myprinter"), get("MYPRINTER"), get("  MyPrinter ") all return the profile.
    - Test 2: names() still contains the case-preserved "MyPrinter" (not a lowercased copy).
    - Test 3: get("nope") still raises ValueError whose message lists available names.
    - Test 4: existing lowercase built-in lookups (e.g. get("juki")) unchanged.
  </behavior>
  <action>
    Per WR-03's "case-insensitive against case-preserved keys" success criterion (FLOW-02): keep `self._profiles` keys verbatim (names()/all()/Select display and the exact-key `profiles.get(...)` path in create_driver_for_selection depend on that). In `__init__`, after the merge, build `self._by_lower: dict[str, str] = {k.lower().strip(): k for k in self._profiles}` (dict comprehension order means later merge entries win on case-fold collision — same last-wins policy the module docstring already documents; no extra handling). Rewrite `get()` to resolve `name.lower().strip()` through `self._by_lower` to the actual key; unknown → the existing ValueError with the available-names list. `_build_index`, `match_vidpid`, `names`, `all` untouched. Update the get() docstring to state the case-preserved-keys/case-insensitive-lookup contract.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry.py -q` exits 0 with the 4 behaviors covered.
    - Behavior assertion: a registry holding an uppercase custom key resolves via get() with any casing, and names() preserves the original casing.
    - Behavior assertion: `--printer MyPrinter` now works because _resolve_profile_selection (cli.py:470) calls registry.get(printer) — no cli.py change needed for FLOW-02.
  </acceptance_criteria>
  <done>Uppercase custom TOML profiles are reachable from setup screen and --printer; registry tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ARCH-04 — explicit profile_name parameter, frozen PrinterSelection, CR-03 defensive cups fallback</name>
  <files>src/claude_teletype/printing/selection.py, src/claude_teletype/printing/discovery.py, src/claude_teletype/cli.py, tests/test_smart_startup.py</files>
  <read_first>
    - .planning/v1.6-ARCHITECTURE-REVIEW.md section "ARCH-04" (source of truth: parameter shape, one call site, freeze recommendation)
    - .planning/v1.6-REVIEW.md section "CR-03" final sentence ("Defensively, create_driver_for_selection should also treat connection_type='cups' with no name as 'pick first enabled queue'")
    - src/claude_teletype/printing/selection.py (match_saved_printer lines 54-94; create_driver_for_selection cups branch lines 156-161 and the USB fallback pattern at 140-155)
    - src/claude_teletype/printing/discovery.py lines 81-88 (PrinterSelection dataclass)
    - src/claude_teletype/cli.py lines 975-998 (smart-startup block; the mutation at 989 and the display re-resolution at 996-997)
    - tests/test_smart_startup.py (existing match_saved_printer + create_driver_for_selection test patterns; tests patch canonical claude_teletype.printing.* paths)
  </read_first>
  <behavior>
    - Test 1: match_saved_printer("usb", "04b8:0005", discovery, profile_name="juki-6100") returns a PrinterSelection with profile_name == "juki-6100"; omitting the kwarg still yields "generic" (existing tests keep passing unmodified).
    - Test 2: match_saved_printer("cups", "HP_LaserJet", discovery, profile_name="epson-fx") stamps profile_name on the cups variant too.
    - Test 3: assigning to a field of a returned PrinterSelection raises dataclasses.FrozenInstanceError.
    - Test 4: create_driver_for_selection with PrinterSelection(connection_type="cups", cups_printer_name=None) and a discovery containing enabled CUPS queues returns CupsPrinterDriver for the first ENABLED queue (disabled skipped); with zero enabled queues it returns NullPrinterDriver.
  </behavior>
  <action>
    selection.py — add keyword parameter `profile_name: str = "generic"` to `match_saved_printer` and pass it into both `PrinterSelection(...)` constructions (replacing the hardcoded "generic"). In `create_driver_for_selection`'s cups branch: when `selection.cups_printer_name` is falsy, pick the first enabled queue from `discovery.cups_printers` (with a stderr note mirroring the USB-fallback message style at lines 150-154); construct `CupsPrinterDriver` from it; only when no enabled queue exists does the existing NullPrinterDriver terminal fallback apply.

    discovery.py — change `@dataclass` to `@dataclass(frozen=True)` on PrinterSelection so the caller-side-mutation class of bug cannot recur (ARCH-04 recommendation; cli.py:989 was the only mutation site per grep of src/ and tests/). If any downstream mutation surfaces during the run, use dataclasses.replace at that site (the project's locked convention for frozen dataclasses) — do NOT unfreeze.

    cli.py — at the smart-startup call site (~982): pass `profile_name=config.saved_printer_profile or "generic"` to match_saved_printer and DELETE the `saved_match.profile_name = ...` mutation line. Leave the status-bar profile re-resolution at 996-997 as-is (separate concern; Phase 34 territory). Preserve the import-locally convention (match_saved_printer is imported inside the branch).
  </action>
  <verify>
    <automated>uv run pytest tests/test_smart_startup.py tests/test_selection_identity.py tests/test_cli.py tests/test_printer_setup.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_smart_startup.py tests/test_selection_identity.py tests/test_cli.py tests/test_printer_setup.py -q` exits 0.
    - Source assertion: `grep -c "saved_match.profile_name =" src/claude_teletype/cli.py` outputs 0.
    - Source assertion: `grep -n "profile_name" src/claude_teletype/printing/selection.py` shows the parameter in match_saved_printer's signature and both constructor uses.
    - Source assertion: `grep -n "frozen=True" src/claude_teletype/printing/discovery.py` hits the PrinterSelection decorator.
    - Behavior assertion: smart-startup reconnect carries the saved profile without any caller-side mutation; a cups selection with no name resolves to the first enabled queue, never silently the simulator.
  </acceptance_criteria>
  <done>match_saved_printer owns the profile hand-off; PrinterSelection is immutable; cups-no-name path degrades loudly to a real queue or (only queueless) to the simulator; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite gate</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/STATE.md "Carry-forward" decisions (write_bytes verbatim channel from Phase 31 must not regress; this plan touches no driver byte path — confirm no drivers.py diff)
  </read_first>
  <action>
    Run the complete test suite. Fix only regressions introduced by this phase's plans (both 32-01 and 32-02 land in wave 1; if 32-01's files show unrelated failures, report rather than cross-edit its files). Confirm `git diff --stat` touches only the files_modified declared across the two plans.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exits 0 with ≥ 912 passing tests.
    - `git diff --stat` (or status) shows no changes to src/claude_teletype/printing/drivers.py or teletype.py (Phase 31 surface untouched).
  </acceptance_criteria>
  <done>Full suite green at ≥ 912 tests; no Phase 31 regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → driver selection | saved_printer_type/id/profile from user config drive reconnect (spoofable only by an actor who already owns the user's config file) |
| custom TOML profiles → registry | user-authored profile names/bytes enter the lookup path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | PrinterSelection hand-off | mitigate | frozen dataclass + parameterized match_saved_printer removes the silent-wrong-profile mutation class (Task 2) |
| T-32-05 | Info Disclosure | registry ValueError listing profile names | accept | names are local user config; message is the documented UX contract |
| T-32-06 | DoS | cups-no-name fallback printing to a wrong first queue | accept | queue set is the user's local cupsd; loud stderr note names the chosen queue |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 with ≥ 912 passing.
- grep gates from Task 2 acceptance criteria all hold.
</verification>

<success_criteria>
- FLOW-02: uppercase custom profile selectable via setup screen and `--printer`.
- FLOW-04: `saved_match.profile_name = ...` mutation gone; profile carried by explicit parameter; PrinterSelection frozen.
- FLOW-01 (factory half): cups selection with no name never silently yields NullPrinterDriver while an enabled queue exists.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/32-setup-detection-flow-fixes/32-02-SUMMARY.md` when done.
</output>
