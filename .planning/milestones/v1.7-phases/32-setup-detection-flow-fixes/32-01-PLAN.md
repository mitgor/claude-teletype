---
phase: 32-setup-detection-flow-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/claude_teletype/screens/printer_setup.py
  - src/claude_teletype/tui.py
  - tests/test_printer_setup_screen.py
  - tests/test_tui.py
autonomous: true
requirements: [FLOW-01, FLOW-03]

must_haves:
  truths:
    - "Kernel-claimed USB printer + accepted CUPS recommendation dismisses PrinterSetupScreen with connection_type='cups' AND a non-None cups_printer_name resolved from discovery.cups_printers"
    - "If the CUPS radio is selected for a USB entry but no enabled CUPS queue exists, Connect refuses to dismiss and writes an error to the diagnostics log"
    - "_save_printer_selection never writes saved_printer_id='' for a usb/cups connection_type"
    - "Under sys.frozen the Install USB Support row is hidden and _install_pyusb refuses to run"
    - "Non-frozen _install_pyusb runs uv sync with cwd pinned to the directory containing the project's pyproject.toml, or refuses with a clear log message when it cannot be located"
  artifacts:
    - path: "src/claude_teletype/screens/printer_setup.py"
      provides: "CR-03 queue resolution in _on_connect; WR-05 frozen + cwd guard in _install_pyusb and install-row visibility"
    - path: "src/claude_teletype/tui.py"
      provides: "Persist guard in _save_printer_selection (no empty saved_printer_id)"
  key_links:
    - from: "src/claude_teletype/screens/printer_setup.py"
      to: "self._discovery.cups_printers"
      via: "queue resolution inside _on_connect when connection_type='cups' and entry type='usb'"
      pattern: "cups_printers"
    - from: "src/claude_teletype/screens/printer_setup.py"
      to: "sys.frozen"
      via: "getattr(sys, \"frozen\", False) guard"
      pattern: "sys, \"frozen\""
---

<objective>
Fix the two setup-screen defects from the v1.6 review: CR-03 (kernel-owns → CUPS
recommendation dismisses with cups_printer_name=None and the simulator silently
takes over, plus the broken empty-ID persist) and WR-05 ("Install USB Support"
runs `uv sync` against arbitrary cwd — wrong or destructive outside the repo,
and useless inside a frozen bundle).

Purpose: FLOW-01 and FLOW-03 — setup always routes the user to the driver they
chose and never mutates a foreign directory's environment.
Output: Patched printer_setup.py + tui.py with regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.6-REVIEW.md            <!-- CR-03 (lines ~80-84) and WR-05 (lines ~112-116) are source of truth -->
@src/claude_teletype/screens/printer_setup.py
@src/claude_teletype/tui.py
@src/claude_teletype/printing/selection.py   <!-- read-only here: mirror the serial-match/first-enabled fallback at lines 140-155; do NOT modify this file (owned by plan 32-02) -->
</context>

<interfaces>
From src/claude_teletype/printing/discovery.py:
```python
@dataclass
class PrinterSelection:
    connection_type: str  # "usb" | "cups" | "skip"
    device_index: int | None = None
    cups_printer_name: str | None = None
    profile_name: str = "generic"
```
`DiscoveryResult.cups_printers` items expose `.name`, `.enabled`, `.serial`, `.model`.
`UsbDeviceInfo` (entry["usb_info"]) exposes `.vendor_id`, `.product_id`, `.serial`, `.product_name`, `.printer_class`.

From src/claude_teletype/screens/printer_setup.py (fix sites):
- `_on_connect` (line ~300): builds PrinterSelection; today `cups_printer_name` is only set when `entry["type"] == "cups"` (line 319-321) — the CR-03 bug.
- `_install_pyusb` (line ~343): `@work` async worker; runs `uv sync --extra usb` with no cwd — the WR-05 bug.
- Install-row visibility set in `on_mount` (line ~217-221) and `_refresh_discovery` (line ~430-432).

From src/claude_teletype/tui.py:
- `_save_printer_selection` (line ~278): writes `cfg.saved_printer_id = ""` in the fallback else branches — the CR-03 persist half.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-03 — resolve a CUPS queue name in _on_connect and guard the empty-ID persist</name>
  <files>src/claude_teletype/screens/printer_setup.py, src/claude_teletype/tui.py, tests/test_printer_setup_screen.py, tests/test_tui.py</files>
  <read_first>
    - .planning/v1.6-REVIEW.md section "CR-03" (source of truth: exact broken flow, fix direction)
    - src/claude_teletype/screens/printer_setup.py (full file — _on_connect, on_option_list_option_selected, _device_entries shape)
    - src/claude_teletype/printing/selection.py lines 140-155 (the serial-match/first-enabled fallback to mirror — do not modify that file in this plan)
    - src/claude_teletype/tui.py lines 269-306 (_save_printer_selection)
    - tests/test_printer_setup_screen.py (existing Textual pilot/screen test patterns to follow)
  </read_first>
  <behavior>
    - Test 1: kernel-owns scenario — USB entry highlighted, CUPS radio selected, discovery has an enabled CUPS queue whose serial equals the USB device's serial → screen dismisses with connection_type="cups" and cups_printer_name == that queue's name (serial match wins over list order).
    - Test 2: same but no serial match → cups_printer_name == first ENABLED queue's name (disabled queues skipped).
    - Test 3: USB entry + CUPS radio + zero enabled CUPS queues → screen does NOT dismiss; an error line appears in the diagnostics Log.
    - Test 4: plain CUPS entry selected → behavior unchanged, cups_printer_name = entry["cups_info"].name.
    - Test 5 (tui.py): _save_printer_selection called with a selection that would produce saved_printer_id="" (e.g. connection_type="cups", cups_printer_name=None) → save_config is NOT called (patch claude_teletype.config.save_config at source module per test convention); a valid selection still persists as before.
  </behavior>
  <action>
    In `_on_connect` (printer_setup.py): when the computed `connection_type == "cups"` and `entry["type"] == "usb"`, resolve the queue name before constructing PrinterSelection: filter `self._discovery.cups_printers` to `q.enabled`; prefer the queue whose `q.serial == entry["usb_info"].serial` (only when that serial is truthy), else the first enabled queue — mirroring the fallback logic in `create_driver_for_selection` (selection.py:140-155). If no enabled queue exists, write an error line to the `#diagnostics-log` Log (e.g. "No enabled CUPS queue available — cannot connect via CUPS") and `return` without dismissing. Keep the existing `entry["type"] == "cups"` path unchanged. The dismissed PrinterSelection for this path must carry `cups_printer_name=<resolved queue name>` and keep `device_index=entry["index"]` and the profile from the Select.

    In `_save_printer_selection` (tui.py): compute the id first; if `selection.connection_type in ("usb", "cups")` and the computed id is empty, return WITHOUT calling save_config (optionally self.notify a warning) — the broken empty-ID state must never reach config. "skip" persists as today.

    Do not touch selection.py in this task (plan 32-02 owns it).
  </action>
  <verify>
    <automated>uv run pytest tests/test_printer_setup_screen.py tests/test_tui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_printer_setup_screen.py tests/test_tui.py -q` exits 0 with the 5 new behaviors covered.
    - Source assertion: `grep -n "cups_printer_name" src/claude_teletype/screens/printer_setup.py` shows the resolved-queue assignment path, not only the `entry["cups_info"].name` conditional.
    - Behavior assertion: selecting CUPS for a kernel-claimed USB device dismisses with connection_type="cups" and a non-None cups_printer_name.
    - Source assertion: tui.py `_save_printer_selection` contains an early return before `save_config` when the id is empty for usb/cups.
  </acceptance_criteria>
  <done>CR-03 flow dismisses with a resolved queue name or refuses with a logged error; empty-ID state can no longer be persisted; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-05 — frozen guard + cwd pinning for Install USB Support</name>
  <files>src/claude_teletype/screens/printer_setup.py, tests/test_printer_setup_screen.py</files>
  <read_first>
    - .planning/v1.6-REVIEW.md section "WR-05" (source of truth)
    - src/claude_teletype/screens/printer_setup.py lines 217-221 (on_mount install-row), 343-381 (_install_pyusb), 430-432 (_refresh_discovery install-row)
    - packaging notes: sys.frozen is set by PyInstaller onedir builds (see .planning/STATE.md PKG context) — no need to read packaging/ sources
  </read_first>
  <behavior>
    - Test 1: with sys.frozen patched truthy, on_mount hides #install-row even when pyusb is unavailable.
    - Test 2: with sys.frozen patched truthy, invoking the install path logs a refusal message and never spawns a subprocess (patch asyncio.create_subprocess_exec / shutil.which to assert no call).
    - Test 3: non-frozen, project root resolvable → uv is invoked with cwd=<directory containing pyproject.toml> (assert the cwd kwarg on the patched create_subprocess_exec).
    - Test 4: non-frozen, no pyproject.toml findable → refusal message logged, no subprocess spawned.
  </behavior>
  <action>
    Add a module-level helper `_project_root() -> Path | None` in printer_setup.py: walk `Path(__file__).resolve().parents` looking for a directory containing `pyproject.toml`; return None if not found (installed-wheel / frozen case). Keep it trivially patchable in tests.

    Install-row visibility: in both `on_mount` and `_refresh_discovery`, hide `#install-row` when `getattr(sys, "frozen", False)` is truthy, in addition to the existing `pyusb_available` condition. When hidden due to frozen with pyusb missing, log a one-line diagnostic instead ("USB support not bundled in this build" or similar).

    `_install_pyusb`: two guards before spawning anything — (1) if `getattr(sys, "frozen", False)`: log "Cannot install USB support inside the packaged app — the bundle's interpreter cannot see a venv install" and return (defense in depth behind the hidden row); (2) `root = _project_root()`; if None: log "Cannot locate the project's pyproject.toml — run 'uv sync --extra usb' from the project directory" and return. Otherwise pass `cwd=str(root)` to `asyncio.create_subprocess_exec`. Everything else (spinner, reimport, refresh) unchanged.

    Threat context (T-32-01): this closes an EoP/Tampering path — package-manager execution against untrusted cwd.
  </action>
  <verify>
    <automated>uv run pytest tests/test_printer_setup_screen.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_printer_setup_screen.py -q` exits 0 with the 4 new behaviors covered.
    - Source assertion: `grep -n 'sys, "frozen"' src/claude_teletype/screens/printer_setup.py` shows guards in install-row visibility AND _install_pyusb.
    - Source assertion: `grep -n "cwd=" src/claude_teletype/screens/printer_setup.py` shows create_subprocess_exec receives an explicit cwd.
    - Behavior assertion: frozen build can never execute `uv sync`; non-frozen never executes it outside the directory owning pyproject.toml.
  </acceptance_criteria>
  <done>Install USB Support is invisible and inert when frozen; when not frozen it only ever syncs the project's own directory or refuses with a clear message; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → shell/cwd | `uv sync` executed by the app against whatever directory the process was launched from (WR-05) |
| discovery data → persisted config | Setup-screen state persisted to the user's config file (CR-03 empty-ID) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering/EoP | printer_setup._install_pyusb | mitigate | frozen guard + cwd pinned to project root containing pyproject.toml; refuse otherwise (Task 2) |
| T-32-02 | Tampering | tui._save_printer_selection | mitigate | never persist saved_printer_id="" for usb/cups (Task 1) |
| T-32-03 | Spoofing | CUPS queue names from discovery | accept | queue names come from the local cupsd the user already trusts; no elevation |
</threat_model>

<verification>
- `uv run pytest tests/test_printer_setup_screen.py tests/test_tui.py -q` exits 0.
- Full suite: `uv run pytest -q` exits 0 with ≥ 912 passing (no regressions; Phase 31 write_bytes contract untouched — this plan modifies no driver code).
</verification>

<success_criteria>
- CR-03: kernel-claimed USB + accepted CUPS recommendation → dismissal carries a resolved queue name; no-queue case refuses to dismiss; empty-ID never persisted.
- WR-05: frozen app hides/refuses install; non-frozen install is cwd-pinned or refuses.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/32-setup-detection-flow-fixes/32-01-SUMMARY.md` when done.
</output>
