---
phase: 08-no-tui-bug-fix
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/claude_teletype/cli.py
  - tests/test_cli.py
autonomous: true
requirements:
  - FIX-01

must_haves:
  truths:
    - "_chat_async completes without crash when stream_claude_response yields StreamResult as final item"
    - "_chat_async prints error message when StreamResult has is_error=True"
    - "_chat_async only passes str items to pace_characters, never StreamResult"
    - "Automated tests cover normal flow, error StreamResult, and empty response in --no-tui path"
  artifacts:
    - path: "src/claude_teletype/cli.py"
      provides: "Fixed _chat_async with StreamResult type dispatch"
      contains: "isinstance(item, StreamResult)"
    - path: "tests/test_cli.py"
      provides: "Test coverage for _chat_async function"
      min_lines: 50
  key_links:
    - from: "src/claude_teletype/cli.py"
      to: "claude_teletype.bridge.StreamResult"
      via: "import and isinstance check"
      pattern: "from claude_teletype\\.bridge import.*StreamResult"
    - from: "src/claude_teletype/cli.py"
      to: "pace_characters"
      via: "only str items passed"
      pattern: "await pace_characters\\(item"
---

<objective>
Fix `_chat_async` crash when `stream_claude_response` yields `StreamResult` in `--no-tui` mode, and add test coverage for the headless code path.

Purpose: `--no-tui` mode is broken since Phase 5 introduced `StreamResult` as the final yield from `stream_claude_response`. The TUI handles this correctly but `_chat_async` was never updated, causing `TypeError: 'StreamResult' object is not iterable` when headless mode is used.

Output: Working `--no-tui` mode with test coverage for normal flow, error StreamResult, and empty response.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-no-tui-bug-fix/08-RESEARCH.md
@src/claude_teletype/cli.py
@src/claude_teletype/bridge.py
@tests/test_bridge.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for _chat_async StreamResult handling</name>
  <files>tests/test_cli.py</files>
  <action>
Create `tests/test_cli.py` with pytest-asyncio tests for the `_chat_async` function. Reuse the NDJSON fixture constants from `tests/test_bridge.py` (import them directly: SYSTEM_INIT, TEXT_DELTA_HELLO, RESULT_MESSAGE_FULL, RESULT_MESSAGE_ERROR).

Write these test cases:

1. **test_chat_async_streams_text_and_handles_stream_result** — Mock `stream_claude_response` to yield `["Hello"]` then a success `StreamResult`. Verify: no crash, `pace_characters` called with `"Hello"` (not StreamResult).

2. **test_chat_async_displays_error_stream_result** — Mock `stream_claude_response` to yield only an error `StreamResult(is_error=True, error_message="rate limit")`. Verify: `console.print` called with error message, no call to `pace_characters`.

3. **test_chat_async_handles_empty_response** — Mock `stream_claude_response` to yield only a success `StreamResult` (no text). Verify: no crash, "No response received" message shown.

Mock strategy (match existing project patterns from test_bridge.py):
- Patch `claude_teletype.cli.stream_claude_response` as an async generator yielding test items
- Patch `claude_teletype.cli.pace_characters` with AsyncMock to capture calls and avoid real delays
- Patch `claude_teletype.cli.console` with MagicMock to capture print calls and suppress Rich spinner
  - `mock_console.status.return_value.__enter__ = MagicMock(return_value=MagicMock())` for context manager
  - `mock_console.status.return_value.__exit__ = MagicMock(return_value=False)`
- Patch `claude_teletype.transcript.make_transcript_output` to return `(MagicMock(), MagicMock())` avoiding filesystem
- Use `tmp_path` fixture for transcript_dir as safety net
- Pass `no_audio=True` to skip audio imports

These tests MUST FAIL on the current code (RED phase). Run `uv run pytest tests/test_cli.py -v` and confirm failures.
  </action>
  <verify>
`uv run pytest tests/test_cli.py -v` runs and all 3 tests FAIL with TypeError or AttributeError related to StreamResult not being handled.
  </verify>
  <done>Three failing tests exist in tests/test_cli.py covering normal flow, error StreamResult, and empty response for _chat_async.</done>
</task>

<task type="auto">
  <name>Task 2: Fix _chat_async and make tests pass</name>
  <files>src/claude_teletype/cli.py</files>
  <action>
Fix `_chat_async` in `src/claude_teletype/cli.py`:

1. **Add import:** Add `StreamResult` to the existing bridge import on line 18:
   ```python
   from claude_teletype.bridge import StreamResult, stream_claude_response
   ```

2. **Add type dispatch in the stream loop** (lines 97-106). Replace the `async for` block with:
   ```python
   async for item in stream_claude_response(prompt):
       if isinstance(item, StreamResult):
           if item.is_error:
               if first_token:
                   status.stop()
               console.print(
                   f"\n[bold red]Error: {item.error_message}"
               )
           break  # StreamResult is always the final yield
       if first_token:
           status.stop()
           first_token = False
       await pace_characters(
           item,
           base_delay_ms=base_delay_ms,
           output_fn=output_fn,
       )
   ```

Key decisions (matching TUI pattern from tui.py line 278):
- Check `isinstance(item, StreamResult)` FIRST, before pace_characters
- `break` after StreamResult since it is always the final item
- For error StreamResult: stop spinner if still active, print error to console
- For success StreamResult: silently consume (no status bar in headless mode)
- Only `str` items reach `pace_characters`

Do NOT change the variable name from `text_chunk` to `item` outside the stream loop — only rename the loop variable.

Run `uv run pytest tests/test_cli.py -v` — all 3 tests must pass (GREEN phase).
Then run `uv run pytest -v` — full suite must still pass (no regressions).
  </action>
  <verify>
`uv run pytest tests/test_cli.py -v` — 3/3 pass.
`uv run pytest -v` — full suite passes with zero failures.
  </verify>
  <done>_chat_async correctly dispatches on StreamResult type; all tests pass including the 3 new ones.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_cli.py -v` — all 3 test cases pass
2. `uv run pytest -v` — full test suite passes (no regressions)
3. `grep -n "isinstance.*StreamResult" src/claude_teletype/cli.py` — shows the type dispatch
4. `grep -n "StreamResult" src/claude_teletype/cli.py` — shows the import
5. `uv run python -c "from claude_teletype.cli import _chat_async; print('import ok')"` — no import errors
</verification>

<success_criteria>
- _chat_async handles StreamResult without crash (the original bug is fixed)
- Error StreamResult produces user-visible error message via console.print
- 3+ new tests cover normal flow, error StreamResult, and empty response
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-no-tui-bug-fix/08-01-SUMMARY.md`
</output>
