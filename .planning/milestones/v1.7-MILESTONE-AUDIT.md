---
milestone: v1.7
audited: 2026-07-19T09:30:00Z
status: passed
scores:
  requirements: 15/15
  phases: 4/4
  integration: 9.5/10
  flows: 6/6
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 33-shared-print-pipeline
    items:
      - "IN-01: picker-mode CLI print skips the transcript entry (pre-existing)"
      - "IN-02: keystrokes typed during a print are dropped from the paper echo"
      - "IN-03: source_path=None with transcript_write raises TypeError after a complete print"
  - phase: 34-architecture-cleanup
    items:
      - "profiles.get_profile is a second parallel lookup kept alive only by tests — migrate tests to ProfileRegistry and delete"
      - "Stale '--juki flag' comment at catalog/juki.py:54"
      - "'juki' alias shadows 'juki-6100' in the exact VID:PID index (benign — bridge VIDs suppressed)"
  - phase: cross-phase (integration audit)
    items:
      - "--printer silently loses to saved profile on smart-startup reconnect (escape hatch: --setup-printer); consider explicit-flag precedence or a startup diagnostic"
      - "CupsPrinterDriver.write text channel is ASCII-replace by design — generic-profile CUPS users typing non-ASCII get '?' silently (codepage path required)"
      - "--no-tui chat path intentionally bypasses the shared document pipeline — needs a guard comment"
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: [31, 32, 33, 34]
  overall: not_applicable
---

# Milestone v1.7 Audit — Review Hardening

**Status: PASSED** — 15/15 requirements satisfied, 4/4 phases verified, integration 9.5/10, 6/6 E2E flows pass, 992 tests green.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION | Traceability | Status |
|--------|-------|--------------|--------------|--------|
| BYTE-01..04 | 31 | passed (4/4 must-haves, code-level evidence) | [x] Complete | satisfied |
| FLOW-01..04 | 32 | passed (11/11 must-haves) | [x] Complete | satisfied |
| PIPE-01..03 | 33 | passed (3/3 must-haves, post-review fixes verified) | [x] Complete | satisfied |
| ARCH-CLEAN-01..04 | 34 | passed (7/7 incl. absorbed 32-review items) | [x] Complete | satisfied |

No orphans: every traceability REQ-ID appears in exactly one phase VERIFICATION with evidence. SUMMARY frontmatter `requirements-completed` was populated only by Phase 34 plans; the stronger VERIFICATION source (code-level greps/tests by the verifier) covers all IDs.

## Phase Verifications

| Phase | Status | Score | Post-review fix pass |
|-------|--------|-------|----------------------|
| 31 Byte Integrity Criticals | passed | 4/4 | n/a (review clean of criticals) |
| 32 Setup & Detection Flow Fixes | passed | 11/11 | warnings deferred by design into Phase 34 (closed there) |
| 33 Shared Print Pipeline | passed | 3/3 | 2 critical + 2 warning review findings fixed pre-verification (Worker.is_finished event guard, quit-during-print join, settings guard, cancellable sleep) |
| 34 Architecture Cleanup | passed | 7/7 | 5 warning review findings fixed pre-verification (fail-loud hardening) |

## Integration (gsd-integration-checker)

Score 9.5/10, no blockers. All seams WIRED:
1. Phase 34 registry threading preserved Phase 32 fixes (case-insensitive lookup is THE lookup everywhere; frozen PrinterSelection; empty-ID persist guard intact)
2. Phase 33 pipeline composes with Phase 31 byte channel end-to-end (render_document → chunk_writes → write_bytes verbatim; 0xb5 round-trip test covers the composed ppds-over-CUPS stack)
3. Diagnostics reach the TUI in every entry path (setup screen notify, smart-startup startup_diagnostics → mount notify, --printer pre-TUI stderr + exit 1)

E2E flows: fresh-start setup ✓, smart-startup reconnect ✓, picker print + escape-cancel + chat-after ✓, CLI print subcommand ✓, typewriter/print mutual exclusion ✓, frozen-app (catalog collect_submodules, no uv sync, usb backend seam) ✓.

Warnings (non-blocking, recorded in tech_debt): --printer vs saved-profile precedence; catalog module count is 9 (not 10 as one summary claimed — doc-only).

## Nyquist Coverage

No VALIDATION.md files exist for phases 31-34. Research was disabled for the whole milestone (config `workflow.research: false`, no RESEARCH.md produced), which per plan-phase §5.5 makes Nyquist validation not applicable for these runs — every plan instead carried `<automated>` verification and each phase closed with a full-suite gate plus a goal-backward verifier pass. Run `/gsd:validate-phase N` retroactively if desired.

## Definition of Done

The milestone goal — "fix everything the v1.6 code + architecture reviews surfaced" — is met: all 3 v1.6 criticals (CR-01/02/03), all 5 warnings scoped in, ARCH-01/02/03/04/06/07/08, and the in-flight review findings of v1.7's own phases (2 criticals, 11 warnings) were fixed with regression tests. Out-of-scope items (ARCH-05 cli.main decomposition, IN-level nits) are recorded in REQUIREMENTS.md Future Requirements.

Tests: 905 → 992 (+87) across the milestone, zero regressions at every merge gate.
