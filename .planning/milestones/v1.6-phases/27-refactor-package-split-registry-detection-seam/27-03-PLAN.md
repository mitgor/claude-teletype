---
phase: 27-refactor-package-split-registry-detection-seam
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - tests/test_printer.py
  - tests/test_profiles.py
  - tests/test_teletype.py
  - tests/test_cli.py
  - tests/test_cli_print.py
  - tests/test_tui.py
  - tests/test_diagnose.py
  - tests/test_markdown.py
  - tests/test_wordwrap.py
  - tests/test_pacer.py
  - tests/test_output.py
  - tests/test_printer_setup_screen.py
  - tests/test_printer_setup.py
  - tests/test_settings_screen.py
  - tests/test_typewriter_screen.py
  - tests/test_file_picker_screen.py
  - tests/test_speed_mode_screen.py
  - tests/test_smart_startup.py
  - src/claude_teletype/printer.py
  - src/claude_teletype/profiles.py
  - src/claude_teletype/markdown.py
  - src/claude_teletype/wordwrap.py
  - src/claude_teletype/pacer.py
  - src/claude_teletype/output.py
  - src/claude_teletype/printer_setup_screen.py
  - src/claude_teletype/settings_screen.py
  - src/claude_teletype/typewriter_screen.py
  - src/claude_teletype/file_picker_screen.py
  - src/claude_teletype/speed_mode_screen.py
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "Every test imports from and patches at the new canonical module path"
    - "No test references an old top-level shim path"
    - "All re-export shims are deleted; old import paths no longer resolve"
    - "Full suite green against canonical paths only"
  artifacts:
    - path: "tests/test_printer.py"
      provides: "Imports + patch targets migrated to printing.{drivers,discovery,selection}"
      contains: "claude_teletype.printing"
  key_links:
    - from: "tests/*"
      to: "claude_teletype.printing.* / .rendering.* / .screens.*"
      via: "import lines and patch() target strings"
      pattern: "patch\\(.claude_teletype\\.(printing|rendering|screens)\\."
---

<objective>
Step 3 (final step) of the move-with-shim package split (REF-01): mechanically migrate all test imports AND all `patch("...")` target strings from the old top-level module paths to the new canonical `printing.*` / `rendering.*` / `screens.*` paths, then DELETE the re-export shims. This is pure, reviewable import churn.

Purpose: Complete the refactor so the codebase has a single canonical import path per symbol, with no shim layer to maintain. Per ARCHITECTURE Step 3, this is the last step — each prior step left the suite green, so any break here is localized to the test migration.
Output: All tests on canonical paths; shims removed; full suite green; package split fully done (REF-01 satisfied).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-refactor-package-split-registry-detection-seam/27-PATTERNS.md
@.planning/research/ARCHITECTURE.md

<environment_note>
Run `pip install 'pytest-asyncio>=1.3.0'` before pytest. True green baseline = 718 passed.
</environment_note>

<migration_table>
From 27-PATTERNS.md "Test Patch-Target Migration" — old module → new canonical path, and the exhaustive list of patch-target strings to rewrite. Key mappings:
- claude_teletype.printer → printing.drivers / printing.discovery / printing.selection (per symbol)
- claude_teletype.profiles → printing.profiles (+ registry, but registry does not exist until Plan 04 — keep profiles symbols on printing.profiles here)
- claude_teletype.markdown/wordwrap/pacer/output → rendering.*
- claude_teletype.{printer_setup,settings,typewriter,file_picker,speed_mode}_screen → screens.{printer_setup,settings,typewriter,file_picker,speed_mode}

Patch-target strings (rewrite to NEW home module; counts are occurrences):
- printer.subprocess ×27, printer.sys ×3 → printing.discovery.{subprocess,sys}
- printer.discover_usb_device ×7, discover_cups_printers ×7, discover_usb_device_verbose ×5, discover_all ×2, _find_usb_printer ×3 → printing.discovery.*
- printer.discover_printer ×30, printer.create_driver_for_selection ×1 → printing.selection.*
- printer.chunk_writes ×1 → printing.drivers.chunk_writes
- profiles.auto_detect_profile ×1 → printing.profiles.auto_detect_profile (NOTE: superseded by registry.match_vidpid in Plan 04; for THIS plan keep it on printing.profiles)
- markdown.MarkdownRenderer ×10 → rendering.markdown.MarkdownRenderer
- pacer.asyncio ×6, pacer.sys ×1 → rendering.pacer.*
- wordwrap.WordWrapper ×2 → rendering.wordwrap.WordWrapper

EXCLUDE any patch target already moved early in Plan 02 (its call-assertion fix) — those are already on the new module; do not double-migrate. Plan 02's SUMMARY lists them.
</migration_table>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test imports and patch targets to canonical paths</name>
  <files>tests/test_printer.py, tests/test_profiles.py, tests/test_teletype.py, tests/test_cli.py, tests/test_cli_print.py, tests/test_tui.py, tests/test_diagnose.py, tests/test_markdown.py, tests/test_wordwrap.py, tests/test_pacer.py, tests/test_output.py, tests/test_printer_setup_screen.py, tests/test_printer_setup.py, tests/test_settings_screen.py, tests/test_typewriter_screen.py, tests/test_file_picker_screen.py, tests/test_speed_mode_screen.py, tests/test_smart_startup.py</files>
  <read_first>
    - tests/test_printer.py, tests/test_teletype.py (highest patch-target volume: subprocess ×27, discover_printer ×30 live here)
    - tests/test_profiles.py, tests/test_cli.py, tests/test_cli_print.py, tests/test_smart_startup.py, tests/test_printer_setup.py, tests/test_printer_setup_screen.py
    - tests/test_markdown.py, tests/test_pacer.py, tests/test_wordwrap.py, tests/test_output.py
    - tests/test_settings_screen.py, tests/test_typewriter_screen.py, tests/test_file_picker_screen.py, tests/test_speed_mode_screen.py, tests/test_tui.py, tests/test_diagnose.py
    - .planning/phases/27-refactor-package-split-registry-detection-seam/27-PATTERNS.md ("Test Patch-Target Migration" — the exhaustive string list)
    - .planning/phases/27-refactor-package-split-registry-detection-seam/27-02-SUMMARY.md (list of patch targets already moved early in Step 2 — do NOT re-migrate these)
  </read_first>
  <action>
    Mechanically rewrite, across the test files, every `from claude_teletype.<old>` import and every `patch("claude_teletype.<old>.<attr>")` / `mocker.patch(...)` / `patch.object` target string to the symbol's NEW canonical module per the migration_table. Map each symbol to the correct sub-module (a discovery function goes to printing.discovery, a driver class to printing.drivers, a selection function to printing.selection — do NOT lump all printer.* onto one module). Keep `auto_detect_profile` patch target on `printing.profiles` for now (registry supersession is Plan 04). Leave `tui.py` / `claude_teletype.tui` references as-is (tui stays top-level this phase). Do not change any assertion, fixture logic, or test name — imports and patch strings ONLY. Skip any patch string already migrated in Step 2 (per Plan 02 SUMMARY).
  </action>
  <verify>
    <automated>pip install 'pytest-asyncio>=1.3.0' -q; python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q` reports 0 failed (718 passed)
    - `grep -rn 'claude_teletype.printer\b\|claude_teletype.profiles\b\|claude_teletype.markdown\b\|claude_teletype.wordwrap\b\|claude_teletype.pacer\b\|claude_teletype.output\b\|_screen\b' tests/ | grep -v 'claude_teletype.tui' | grep -vE 'printing|rendering|screens'` returns nothing (no test references an old shim path; tui excepted)
    - `grep -rc 'patch(.claude_teletype.printing.discovery.subprocess' tests/ | grep -v ':0' | wc -l` shows the subprocess patches landed on printing.discovery
    - No assertion or fixture body changed (diff is import lines + patch-string args only)
  </acceptance_criteria>
  <done>Every test imports and patches at the canonical printing/rendering/screens path; full suite green on new paths.</done>
</task>

<task type="auto">
  <name>Task 2: Delete re-export shims and confirm old paths no longer resolve</name>
  <files>src/claude_teletype/printer.py, src/claude_teletype/profiles.py, src/claude_teletype/markdown.py, src/claude_teletype/wordwrap.py, src/claude_teletype/pacer.py, src/claude_teletype/output.py, src/claude_teletype/printer_setup_screen.py, src/claude_teletype/settings_screen.py, src/claude_teletype/typewriter_screen.py, src/claude_teletype/file_picker_screen.py, src/claude_teletype/speed_mode_screen.py</files>
  <read_first>
    - src/claude_teletype/printer.py, profiles.py, markdown.py, wordwrap.py, pacer.py, output.py (current shim contents)
    - src/claude_teletype/printer_setup_screen.py, settings_screen.py, typewriter_screen.py, file_picker_screen.py, speed_mode_screen.py (current shim contents)
    - .planning/research/ARCHITECTURE.md (Section B Step 4 — shim deletion is optional but chosen here for a single canonical path)
  </read_first>
  <action>
    Delete the eleven re-export shim files (the six top-level `printer.py`, `profiles.py`, `markdown.py`, `wordwrap.py`, `pacer.py`, `output.py` AND the five `*_screen.py` shims) now that no production code (Plan 02) and no test (Task 1 above) references them. Do NOT delete `tui.py` — it remains a real top-level module owning TeletypeApp + ConfirmSwapScreen this phase. After deletion, run the full suite to prove nothing resolved through a shim. If any import error surfaces, it is a missed migration from Task 1 — fix the offending import/patch string, do not re-add the shim.
  </action>
  <verify>
    <automated>pip install 'pytest-asyncio>=1.3.0' -q; python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/claude_teletype/printer.py && test ! -f src/claude_teletype/profiles.py && test ! -f src/claude_teletype/markdown.py && test ! -f src/claude_teletype/printer_setup_screen.py` exits 0 (shims gone)
    - `test -f src/claude_teletype/tui.py` exits 0 (tui.py retained)
    - `python -c "import claude_teletype.printer" 2>&1 | grep -q ModuleNotFoundError` (old path no longer resolves)
    - `python -m pytest -q` reports 0 failed (718 passed)
    - `ruff check src/claude_teletype tests` passes (no F401/unused imports left from removed shims)
  </acceptance_criteria>
  <done>All shims deleted; old top-level import paths no longer resolve; tui.py retained; full suite green on canonical paths. REF-01 package split complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test↔import graph | Test import/patch migration; no external input. Risk: a missed patch string leaves a stale mock that no-ops silently, or shim deletion breaks an unmigrated import. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Spoofing | Missed patch-target string → mock patches a non-existent/old name | mitigate | grep gate proves no old shim path remains in tests/; full suite green required; shim deletion forces any miss to surface as ImportError |
| T-27-06 | Repudiation | Behavior change hidden in "pure churn" commit | mitigate | Diff restricted to import lines + patch arg strings; assertions/fixtures unchanged; 718-test green-gate |
| T-27-SC | Tampering | pip installs | accept | Only `pytest-asyncio` (declared dev dependency) |
</threat_model>

<verification>
- Full suite green: `python -m pytest -q` → 0 failed on canonical paths only
- No test references an old shim path (grep gate, tui excepted)
- Old top-level modules deleted; `import claude_teletype.printer` raises ModuleNotFoundError
- tui.py retained and functional
</verification>

<success_criteria>
- All tests import + patch at canonical printing/rendering/screens paths
- Re-export shims deleted; single canonical import path per symbol
- tui.py remains top-level
- Full 718-test suite green; REF-01 package split complete
</success_criteria>

<output>
Create `.planning/phases/27-refactor-package-split-registry-detection-seam/27-03-SUMMARY.md` when done
</output>
