---
phase: 27-refactor-package-split-registry-detection-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/claude_teletype/printing/__init__.py
  - src/claude_teletype/printing/drivers.py
  - src/claude_teletype/printing/discovery.py
  - src/claude_teletype/printing/selection.py
  - src/claude_teletype/printing/profiles.py
  - src/claude_teletype/rendering/__init__.py
  - src/claude_teletype/rendering/markdown.py
  - src/claude_teletype/rendering/wordwrap.py
  - src/claude_teletype/rendering/pacer.py
  - src/claude_teletype/rendering/output.py
  - src/claude_teletype/screens/__init__.py
  - src/claude_teletype/screens/printer_setup.py
  - src/claude_teletype/screens/settings.py
  - src/claude_teletype/screens/typewriter.py
  - src/claude_teletype/screens/file_picker.py
  - src/claude_teletype/screens/speed_mode.py
  - src/claude_teletype/printer.py
  - src/claude_teletype/profiles.py
  - src/claude_teletype/markdown.py
  - src/claude_teletype/wordwrap.py
  - src/claude_teletype/pacer.py
  - src/claude_teletype/output.py
  - src/claude_teletype/printer_setup_screen.py
  - src/claude_teletype/settings_screen.py
  - src/claude_teletype/typewriter_screen.py
  - src/claude_teletype/file_picker_screen.py
  - src/claude_teletype/speed_mode_screen.py
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "The full test suite passes unchanged after the physical move (re-export shims keep every old import path alive)"
    - "All public symbols importable from BOTH old paths (claude_teletype.printer) and new paths (claude_teletype.printing.drivers)"
    - "No internal import or test file was edited in this step"
  artifacts:
    - path: "src/claude_teletype/printing/drivers.py"
      provides: "Driver classes + Protocol moved from printer.py"
      contains: "class ProfilePrinterDriver"
    - path: "src/claude_teletype/printing/discovery.py"
      provides: "Discovery functions + dataclasses moved from printer.py"
      contains: "def discover_all"
    - path: "src/claude_teletype/printing/selection.py"
      provides: "Selection/driver-factory functions moved from printer.py"
      contains: "def create_driver_for_selection"
    - path: "src/claude_teletype/printing/profiles.py"
      provides: "PrinterProfile + loaders moved from profiles.py"
      contains: "class PrinterProfile"
    - path: "src/claude_teletype/printer.py"
      provides: "Re-export shim preserving old import path"
      contains: "from claude_teletype.printing.drivers import"
  key_links:
    - from: "src/claude_teletype/printer.py"
      to: "src/claude_teletype/printing/drivers.py"
      via: "explicit re-export of named symbols"
      pattern: "from claude_teletype.printing.drivers import"
    - from: "src/claude_teletype/screens/__init__.py"
      to: "tui.py ConfirmSwapScreen importability"
      via: "screens package exports do not break existing test imports"
      pattern: "ConfirmSwapScreen"
---

<objective>
Step 1 of the move-with-shim package split (REF-01): physically move modules into `printing/`, `rendering/`, and `screens/` sub-packages, then leave thin re-export shims at every old top-level module path. ZERO internal-import edits, ZERO test edits in this step.

Purpose: Decouple the risky physical move from the noisy import churn (Steps 2 and 3) so any breakage is localized to the step that caused it. The shims keep all 718 absolute-path-import tests green across the move.
Output: New `printing/`, `rendering/`, `screens/` packages holding the moved code; old module paths become re-export shims; full suite still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-refactor-package-split-registry-detection-seam/27-CONTEXT.md
@.planning/phases/27-refactor-package-split-registry-detection-seam/27-PATTERNS.md
@.planning/research/ARCHITECTURE.md

<environment_note>
The test suite requires `pytest-asyncio` (declared in the `dev` dependency-group of pyproject.toml but NOT installed in a bare venv). Before running pytest, run `pip install 'pytest-asyncio>=1.3.0'` (or `uv sync --group dev`). Without it ~99 async TUI tests fail spuriously with "Unknown pytest.mark.asyncio". The TRUE green baseline is 718 passed.
</environment_note>

<symbol_map>
Symbol→destination mapping is in 27-PATTERNS.md "Pattern Assignments". Summary:
- printer.py:16-418 → printing/drivers.py (PrinterDriver Protocol, NullPrinterDriver, FilePrinterDriver, CupsPrinterDriver, UsbPrinterDriver, ProfilePrinterDriver, JukiPrinterDriver, A4_COLUMNS, make_printer_output, chunk_writes)
- printer.py:47-90,447-801 → printing/discovery.py (UsbDeviceInfo, CupsPrinterInfo, DiscoveryResult, PrinterSelection, _find_usb_printer, kernel_driver_holds_printer, discover_usb_device, discover_usb_device_verbose, discover_macos_usb_printers, discover_cups_printers, discover_all)
- printer.py:804-941 → printing/selection.py (select_printer, match_saved_printer, create_driver_for_selection, discover_printer)
- profiles.py (whole) → printing/profiles.py
- markdown.py / wordwrap.py / pacer.py / output.py → rendering/ (1:1)
- printer_setup_screen.py / settings_screen.py / typewriter_screen.py / file_picker_screen.py / speed_mode_screen.py → screens/ (renamed: drop the `_screen` suffix per ARCHITECTURE target map)
</symbol_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move printer.py and profiles.py into printing/ with re-export shims</name>
  <files>src/claude_teletype/printing/__init__.py, src/claude_teletype/printing/drivers.py, src/claude_teletype/printing/discovery.py, src/claude_teletype/printing/selection.py, src/claude_teletype/printing/profiles.py, src/claude_teletype/printer.py, src/claude_teletype/profiles.py</files>
  <read_first>
    - src/claude_teletype/printer.py (full — see exact split boundaries: drivers 16-418, discovery 47-90 + 447-801, selection 804-941)
    - src/claude_teletype/profiles.py (full — moves wholesale)
    - .planning/phases/27-refactor-package-split-registry-detection-seam/27-PATTERNS.md ("Re-export shim" section — the exact shim pattern and the explicit-re-export trap)
    - .planning/research/ARCHITECTURE.md (Section B — migration strategy)
  </read_first>
  <action>
    Create `src/claude_teletype/printing/__init__.py`. Split `printer.py` into three new modules under `printing/`: `drivers.py`, `discovery.py`, `selection.py`, per the symbol_map. Move `profiles.py` wholesale to `printing/profiles.py`. Use `git mv` where a file moves 1:1 (profiles.py) to preserve blame; for the printer.py three-way split, create the new files and reduce printer.py to a shim.

    Within the moved files, repoint ONLY the imports that cross the new internal boundary so the moved modules import from each other correctly: in `drivers.py` change `from claude_teletype.profiles import PrinterProfile, get_profile` to `from claude_teletype.printing.profiles import PrinterProfile, get_profile`; in `discovery.py`/`selection.py` repoint any sibling references (e.g. selection.py imports driver classes and discovery dataclasses) to `claude_teletype.printing.{drivers,discovery}`. Place `PrinterSelection` in `discovery.py` (it is consumed by both selection.py and screens/printer_setup.py — discovery.py avoids a cycle). Do NOT touch the `subprocess`/`sys` module-level imports' usage — they move WITH the discovery functions that call them (drivers/discovery keep their own `import subprocess`/`import sys` at module top as the source had).

    Make the old `printer.py` a pure re-export shim: `from claude_teletype.printing.drivers import *` (and discovery, selection) plus the EXPLICIT named re-export block listed in 27-PATTERNS.md "Re-export shim" (PrinterDriver, NullPrinterDriver, ProfilePrinterDriver, FilePrinterDriver, CupsPrinterDriver, UsbPrinterDriver, JukiPrinterDriver, make_printer_output, chunk_writes, A4_COLUMNS; UsbDeviceInfo, CupsPrinterInfo, DiscoveryResult, PrinterSelection, discover_all, discover_cups_printers, discover_usb_device, discover_usb_device_verbose, discover_macos_usb_printers, kernel_driver_holds_printer, _find_usb_printer; select_printer, match_saved_printer, create_driver_for_selection, discover_printer). Add `# noqa: F401,F403` on the star lines. Make the old `profiles.py` a shim that does `from claude_teletype.printing.profiles import *` plus an explicit re-export of `PrinterProfile, BUILTIN_PROFILES, resolve_style, load_custom_profiles, auto_detect_profile, get_profile`.

    Do NOT yet move the `subprocess`/`sys` patch targets to the shim — Step 2 (Plan 02) handles the patch-target migration risk. In Step 1 the shim's `import *` is sufficient because every test still import-resolves; the patch-call-assertion validation happens in Plan 02/03.
  </action>
  <verify>
    <automated>pip install 'pytest-asyncio>=1.3.0' -q; python -m pytest tests/test_printer.py tests/test_profiles.py tests/test_teletype.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from claude_teletype.printer import ProfilePrinterDriver, discover_all, create_driver_for_selection, PrinterSelection, chunk_writes"` exits 0
    - `python -c "from claude_teletype.printing.drivers import ProfilePrinterDriver; from claude_teletype.printing.discovery import discover_all, PrinterSelection; from claude_teletype.printing.selection import create_driver_for_selection"` exits 0
    - `python -c "from claude_teletype.profiles import PrinterProfile, BUILTIN_PROFILES, get_profile; from claude_teletype.printing.profiles import PrinterProfile"` exits 0
    - `grep -n 'from claude_teletype.printing.drivers import' src/claude_teletype/printer.py` returns at least the explicit re-export line
    - tests/test_printer.py, tests/test_profiles.py, tests/test_teletype.py all pass (no test file edited)
    - `git status` shows tests/ unchanged
  </acceptance_criteria>
  <done>printer.py and profiles.py code lives under printing/; old paths are shims; the three printer-centric + profiles + teletype test modules pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Move rendering and screen modules into rendering/ and screens/ with shims</name>
  <files>src/claude_teletype/rendering/__init__.py, src/claude_teletype/rendering/markdown.py, src/claude_teletype/rendering/wordwrap.py, src/claude_teletype/rendering/pacer.py, src/claude_teletype/rendering/output.py, src/claude_teletype/screens/__init__.py, src/claude_teletype/screens/printer_setup.py, src/claude_teletype/screens/settings.py, src/claude_teletype/screens/typewriter.py, src/claude_teletype/screens/file_picker.py, src/claude_teletype/screens/speed_mode.py, src/claude_teletype/markdown.py, src/claude_teletype/wordwrap.py, src/claude_teletype/pacer.py, src/claude_teletype/output.py, src/claude_teletype/printer_setup_screen.py, src/claude_teletype/settings_screen.py, src/claude_teletype/typewriter_screen.py, src/claude_teletype/file_picker_screen.py, src/claude_teletype/speed_mode_screen.py</files>
  <read_first>
    - src/claude_teletype/markdown.py, src/claude_teletype/wordwrap.py, src/claude_teletype/pacer.py, src/claude_teletype/output.py (the four rendering modules — moved 1:1)
    - src/claude_teletype/printer_setup_screen.py, settings_screen.py, typewriter_screen.py, file_picker_screen.py, speed_mode_screen.py (the five screen modules)
    - src/claude_teletype/tui.py lines 1-60 (to confirm which screen classes tui.py imports, e.g. ConfirmSwapScreen ownership — per 27-PATTERNS.md note, ConfirmSwapScreen stays with tui.py; tui.py stays top-level this phase)
    - tests/test_settings_screen.py (imports ConfirmSwapScreen — must stay importable)
    - .planning/phases/27-refactor-package-split-registry-detection-seam/27-PATTERNS.md ("Moved modules" table — note screen files drop the `_screen` suffix)
  </read_first>
  <action>
    Create `src/claude_teletype/rendering/__init__.py` and `src/claude_teletype/screens/__init__.py`. `git mv` the four rendering modules to `rendering/markdown.py`, `rendering/wordwrap.py`, `rendering/pacer.py`, `rendering/output.py` (1:1, no suffix change). `git mv` the five screen modules to `screens/printer_setup.py`, `screens/settings.py`, `screens/typewriter.py`, `screens/file_picker.py`, `screens/speed_mode.py` (DROP the `_screen` suffix to match the ARCHITECTURE target map).

    Within the moved screen modules, repoint sibling imports to the new canonical paths where a screen imports another moved module (e.g. a screen importing `claude_teletype.markdown` becomes `claude_teletype.rendering.markdown`; a screen importing `claude_teletype.printer` symbols may stay on the shim for now — Step 2 repoints those). Keep cross-package imports resolving; do not edit non-moved modules.

    Leave a re-export shim at each old path: `markdown.py` does `from claude_teletype.rendering.markdown import *` + explicit re-export of `MarkdownRenderer` (and any other public names); same pattern for wordwrap (`WordWrapper`), pacer, output. Each old `*_screen.py` does `from claude_teletype.screens.<newname> import *` + explicit re-export of the screen class(es) it defined. CRITICAL: `tui.py` and any inline helper screens it owns (e.g. `ConfirmSwapScreen`) stay in `tui.py` at top level this phase — do NOT move tui.py. Confirm `from claude_teletype.tui import ConfirmSwapScreen` still resolves.
  </action>
  <verify>
    <automated>python -m pytest tests/test_markdown.py tests/test_wordwrap.py tests/test_pacer.py tests/test_output.py tests/test_printer_setup_screen.py tests/test_settings_screen.py tests/test_typewriter_screen.py tests/test_file_picker_screen.py tests/test_speed_mode_screen.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from claude_teletype.markdown import MarkdownRenderer; from claude_teletype.rendering.markdown import MarkdownRenderer"` exits 0
    - `python -c "from claude_teletype.wordwrap import WordWrapper; from claude_teletype.rendering.wordwrap import WordWrapper"` exits 0
    - `python -c "from claude_teletype.printer_setup_screen import *; from claude_teletype.screens.printer_setup import *"` exits 0
    - `python -c "from claude_teletype.settings_screen import *"` exits 0 AND `python -c "from claude_teletype.tui import ConfirmSwapScreen"` exits 0
    - All nine listed rendering + screen test modules pass (no test file edited)
  </acceptance_criteria>
  <done>rendering/ and screens/ packages hold the moved code; old module paths are shims; tui.py untouched at top level; ConfirmSwapScreen still importable; the nine rendering+screen test modules pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Define printing/__init__ public API and confirm full-suite green</name>
  <files>src/claude_teletype/printing/__init__.py, src/claude_teletype/rendering/__init__.py, src/claude_teletype/screens/__init__.py</files>
  <read_first>
    - src/claude_teletype/printing/__init__.py (the stub created in Task 1 — now populate it)
    - src/claude_teletype/printing/drivers.py, discovery.py, selection.py, profiles.py (to enumerate the public names to re-export)
    - .planning/research/ARCHITECTURE.md (Section B — "printing/__init__.py re-exports public API")
  </read_first>
  <action>
    Populate `printing/__init__.py` to re-export the printing package's public surface (driver classes, discovery functions + dataclasses, selection functions, PrinterProfile + profile loaders) so `from claude_teletype.printing import X` works for Step 2 internal repointing. Keep `rendering/__init__.py` and `screens/__init__.py` minimal (empty or thin re-exports — they are namespace packages this step). Run the FULL suite to prove the move is behavior-neutral. The gate to finish Step 1 is a fully-green 718-test suite.
  </action>
  <verify>
    <automated>pip install 'pytest-asyncio>=1.3.0' -q; python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q` reports `718 passed` (or current full count, 0 failed)
    - `python -c "from claude_teletype.printing import ProfilePrinterDriver, discover_all, create_driver_for_selection, PrinterProfile"` exits 0
    - `git status` shows zero modifications under tests/
    - `ruff check src/claude_teletype/printing src/claude_teletype/rendering src/claude_teletype/screens` reports no F-category (undefined/unused-import) errors beyond the intentional F401/F403 noqa shims
  </acceptance_criteria>
  <done>Full suite green; printing package exposes its public API; no test or behavior changed. Step 1 complete and ready for Step 2 internal repointing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import graph (internal) | Refactor of module boundaries; no external/network input. Risk is structural (broken imports / silent behavior drift), not adversarial. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | Behavior drift during physical move | mitigate | Full 718-test suite green-gate at Task 3; shims re-export identical symbols (no logic edits in moved code) |
| T-27-02 | Tampering | Atomic CR+LF+reinit USB transfer (locked invariant in ProfilePrinterDriver) accidentally fragmented during drivers.py extraction | mitigate | ProfilePrinterDriver code moved verbatim (no edits to printer.py:286-340 logic); test_printer.py asserts the transfer path |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | Only install is `pytest-asyncio` (already a declared dev dependency-group member in pyproject.toml — legitimate, no new package introduced) |
</threat_model>

<verification>
- Full suite: `python -m pytest -q` → 718 passed, 0 failed (after `pip install 'pytest-asyncio>=1.3.0'`)
- Old import paths resolve: spot-check imports from claude_teletype.printer / .profiles / .markdown / .printer_setup_screen
- New import paths resolve: spot-check imports from claude_teletype.printing.* / .rendering.* / .screens.*
- No test files modified: `git diff --stat tests/` is empty
</verification>

<success_criteria>
- printing/, rendering/, screens/ packages exist holding the moved code
- Every old top-level module path is a re-export shim that keeps old imports working
- tui.py remains top-level; ConfirmSwapScreen still importable from claude_teletype.tui
- Full 718-test suite passes with zero test-file or behavior changes
</success_criteria>

<output>
Create `.planning/phases/27-refactor-package-split-registry-detection-seam/27-01-SUMMARY.md` when done
</output>
