---
phase: 27-refactor-package-split-registry-detection-seam
plan: 06
type: execute
wave: 4
depends_on: ["27-03"]
files_modified:
  - tests/test_codepage.py
  - tests/fixtures/custom_codepage_profile.toml
autonomous: true
requirements: [DIR-01]
must_haves:
  truths:
    - "codepage_command, text_codec, and text_fallback are settable from a custom TOML profile and parsed into the PrinterProfile"
    - "The codepage consumption path (text_fallback → codec gate → lazy codepage_command emission) is test-covered"
    - "The atomic CR+LF+reinit transfer remains a single write (locked invariant) and is asserted"
  artifacts:
    - path: "tests/test_codepage.py"
      provides: "Coverage for the three codepage fields: TOML load + driver consumption"
      contains: "def test_"
    - path: "tests/fixtures/custom_codepage_profile.toml"
      provides: "A custom-TOML profile exercising codepage_command/text_codec/text_fallback"
      contains: "codepage_command"
  key_links:
    - from: "tests/fixtures/custom_codepage_profile.toml"
      to: "load_custom_profiles → PrinterProfile"
      via: "hex→bytes for codepage_command, str for text_codec, table for text_fallback"
      pattern: "load_custom_profiles"
    - from: "ProfilePrinterDriver"
      to: "codepage_command emission"
      via: "lazy _ensure_codepage on first non-ASCII write"
      pattern: "codepage"
---

<objective>
Formalize the already-shipped codepage work (DIR-01): the `codepage_command` / `text_codec` / `text_fallback` fields exist on PrinterProfile (commits d70aded/7ccdff5) and `load_custom_profiles` already parses them — but they are untracked and untested. This plan adds test coverage proving they load from custom TOML and drive the ProfilePrinterDriver codepage path correctly, plus a fixture demonstrating custom-TOML support.

Purpose: Other printer families (Phase 29) will extend codepage behavior nine more times. Before being extended, this behavior must be tracked + tested so a regression is caught. This is test/TOML formalization ONLY — the fields and parse logic already ship; this plan adds NO new production fields and changes NO behavior.
Output: `tests/test_codepage.py` covering load + consumption; a custom-codepage TOML fixture; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-refactor-package-split-registry-detection-seam/27-PATTERNS.md

<environment_note>
Run `pip install 'pytest-asyncio>=1.3.0'` before pytest. True green baseline = 718 passed.
</environment_note>

<existing_behavior>
From 27-PATTERNS.md "Additive frozen-field extension" + "printing/drivers.py" codepage notes — the behavior already lives in the codebase (paths below assume Plan 03 moved them to printing/):

- Fields on PrinterProfile (printing/profiles.py, was profiles.py:97-111): `codepage_command: bytes`, `text_codec: str`, `text_fallback: dict`.
- Parse in load_custom_profiles (printing/profiles.py, was profiles.py:389-391): `codepage_command=bytes.fromhex(data.get("codepage_command", ""))`, `text_codec=data.get("text_codec", "")`, `text_fallback=dict(data.get("text_fallback", {}))`.
- Consumption in ProfilePrinterDriver (printing/drivers.py, was printer.py:286-340):
  * text_fallback applied per-char BEFORE codec (was printer.py:286-289)
  * _needs_codepage() gates on text_codec (was printer.py:316-326)
  * _ensure_codepage() lazily emits codepage_command once (was printer.py:328-340)
  * atomic CR+LF+reinit transfer (was printer.py:290-301) — the LOCKED invariant; must remain a single write.

This plan reads those paths to understand them; it does NOT modify them. Verify the exact current line locations after the Plan 03 move before asserting against them.
</existing_behavior>

<scope_guard>
DIR-01 is test + custom-TOML coverage ONLY. Do NOT add or rename PrinterProfile fields. Do NOT modify load_custom_profiles parse logic. Do NOT modify ProfilePrinterDriver. Do NOT touch printing/profiles.py or printing/drivers.py source (Plan 04 owns the profiles.py edits for the registry; this plan must not collide). Existing example assets (examples/vader-ballada1.md and the CP866/CP1125 ballads) demonstrate the feature for reference but are not test fixtures — create a dedicated TOML fixture under tests/fixtures/.
</scope_guard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom-TOML codepage fixture + load coverage</name>
  <files>tests/test_codepage.py, tests/fixtures/custom_codepage_profile.toml</files>
  <read_first>
    - src/claude_teletype/printing/profiles.py (PrinterProfile field definitions for codepage_command/text_codec/text_fallback; load_custom_profiles parse lines — confirm the three decoding conventions: bytes.fromhex for codepage_command, plain str for text_codec, dict for text_fallback)
    - tests/test_profiles.py (existing custom-TOML load test patterns — replicate the fixture-loading + assertion style)
    - examples/vader-ballada1.md and the commit d70aded/7ccdff5 codepage code (reference for realistic CP866/CP1125 values to put in the fixture)
  </read_first>
  <action>
    Create `tests/fixtures/custom_codepage_profile.toml` defining a `[printer.profiles.<name>]` entry that sets `codepage_command` (hex string, e.g. an `ESC t n` style sequence), `text_codec` (e.g. "cp866"), and a `text_fallback` table mapping at least one source char to a replacement. Create `tests/test_codepage.py` with tests that load this fixture through `load_custom_profiles` and assert: codepage_command parsed via `bytes.fromhex` into the expected bytes, text_codec stored as the exact string, text_fallback stored as the expected dict. Mirror the fixture-loading style already used in test_profiles.py. Use the canonical import path `from claude_teletype.printing.profiles import load_custom_profiles, PrinterProfile`.
  </action>
  <verify>
    <automated>pip install 'pytest-asyncio>=1.3.0' -q; python -m pytest tests/test_codepage.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/custom_codepage_profile.toml exists and `grep -q codepage_command tests/fixtures/custom_codepage_profile.toml`
    - test_codepage.py loads the fixture and asserts codepage_command == the expected `bytes.fromhex(...)` value
    - test asserts text_codec equals the literal string and text_fallback equals the expected dict
    - tests pass
  </acceptance_criteria>
  <done>A custom-TOML profile with all three codepage fields loads correctly and is asserted field-by-field.</done>
</task>

<task type="auto">
  <name>Task 2: ProfilePrinterDriver codepage-consumption + atomic-transfer coverage</name>
  <files>tests/test_codepage.py</files>
  <read_first>
    - src/claude_teletype/printing/drivers.py (ProfilePrinterDriver: the text_fallback per-char application, _needs_codepage gate, _ensure_codepage lazy emission, and the atomic CR+LF+reinit write — confirm exact method names and the single-write invariant after the Plan 03 move)
    - tests/test_printer.py (existing ProfilePrinterDriver test patterns — how a fake/capturing write target is wired)
    - src/claude_teletype/printing/profiles.py (to build a PrinterProfile in-test with codepage fields set)
  </read_first>
  <action>
    Add tests to `tests/test_codepage.py` that construct a ProfilePrinterDriver wrapping a capturing sink, using a PrinterProfile with text_codec + codepage_command + a text_fallback entry set. Assert: (1) writing a non-ASCII char that has a text_fallback mapping emits the fallback substitution BEFORE codec encoding; (2) the codepage_command is emitted lazily exactly ONCE on the first write that needs it (not on plain-ASCII writes, not repeated on subsequent writes); (3) a `'\n'` write produces the CR+LF+reinit sequence as a SINGLE atomic write call to the sink (the locked invariant) — assert the captured write list shows one combined transfer for the newline, not fragmented LF. Drive these against the real ProfilePrinterDriver; do not modify it.
  </action>
  <verify>
    <automated>pip install 'pytest-asyncio>=1.3.0' -q; python -m pytest tests/test_codepage.py -q && python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test asserts text_fallback is applied per-char before codec encoding
    - test asserts codepage_command is emitted exactly once (lazy, gated, non-repeating) — e.g. count of the codepage_command bytes in the captured output == 1 across multiple non-ASCII writes
    - test asserts the newline path is a single atomic write (CR+LF+reinit not fragmented) — the locked invariant
    - `python -m pytest -q` reports 0 failed (718 + new codepage tests)
    - no source file under src/claude_teletype/printing/ was modified by this plan (`git status` shows only tests/ additions)
  </acceptance_criteria>
  <done>The codepage consumption path and the atomic newline invariant are test-covered without modifying production code; full suite green. DIR-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| custom TOML → driver bytes | User codepage hex/codec/fallback values flow to the printer. Risk is malformed bytes, not adversarial — parsing already validated upstream; this plan only adds coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Tampering | Future codepage extension (Phase 29) silently breaking the lazy-once emission or the atomic newline | mitigate | This plan locks both behaviors behind tests now, before nine families extend them |
| T-27-11 | Tampering | This plan accidentally editing production codepage code | mitigate | scope_guard forbids src/ edits; acceptance criterion asserts `git status` shows only tests/ changes |
| T-27-SC | Tampering | pip installs | accept | Only `pytest-asyncio` (declared dev dependency) |
</threat_model>

<verification>
- Codepage tests: `python -m pytest tests/test_codepage.py -q` all pass
- Full suite: `python -m pytest -q` → 0 failed
- No production source modified: `git status` shows only tests/ additions
- Atomic CR+LF+reinit single-write invariant asserted
</verification>

<success_criteria>
- codepage_command/text_codec/text_fallback are settable from custom TOML and asserted on load
- The driver consumption path (fallback → codec gate → lazy-once codepage emission) is test-covered
- The atomic CR+LF+reinit newline invariant is asserted
- No production code modified; full suite green. DIR-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/27-refactor-package-split-registry-detection-seam/27-06-SUMMARY.md` when done
</output>
