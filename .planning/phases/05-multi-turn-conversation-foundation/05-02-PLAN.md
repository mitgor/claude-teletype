---
phase: 05-multi-turn-conversation-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/claude_teletype/tui.py
  - tests/test_tui.py
autonomous: true
requirements:
  - CONV-01
  - CONV-03
  - CONV-04
  - CONV-05

must_haves:
  truths:
    - "User can submit multiple prompts in TUI and each response includes context from previous turns"
    - "User sees 'You: ' label before their prompt and 'Claude: ' label before response in the output pane"
    - "User sees blank-line separators between conversation turns"
    - "User sees a status bar below input showing turn count, context %, and model name"
    - "User cannot type or submit while Claude is streaming (input is disabled and dimmed)"
    - "User can press Escape to cancel a streaming response, which appends '[interrupted]' to the output"
    - "Printer and transcript output matches TUI format (same 'You: '/'Claude: ' labels and blank lines)"
  artifacts:
    - path: "src/claude_teletype/tui.py"
      provides: "Multi-turn TUI with session state, turn formatting, status bar, input blocking, escape cancel"
      contains: "session_id"
    - path: "tests/test_tui.py"
      provides: "Tests for turn formatting, status bar, input blocking"
  key_links:
    - from: "src/claude_teletype/tui.py"
      to: "src/claude_teletype/bridge.py"
      via: "stream_claude_response(prompt, session_id=self._session_id, proc_holder=self._proc_holder)"
      pattern: "stream_claude_response.*session_id"
    - from: "src/claude_teletype/tui.py"
      to: "src/claude_teletype/bridge.py"
      via: "isinstance(item, StreamResult) check for session metadata"
      pattern: "isinstance.*StreamResult"
---

<objective>
Transform the TUI from a one-shot prompt/response interface into a persistent multi-turn conversation with turn formatting, status bar, input blocking during streaming, and Escape-to-cancel. The TUI stores session_id from StreamResult and passes it on each subsequent prompt via --resume, giving Claude full conversation context.

Purpose: This is the core user-facing feature of Phase 5 -- the user can now have a real conversation with Claude in the TUI, with clear attribution labels, status metadata, and safe input handling.
Output: Updated tui.py with all multi-turn features. Updated test_tui.py with new tests.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-turn-conversation-foundation/05-RESEARCH.md
@.planning/phases/05-multi-turn-conversation-foundation/05-01-SUMMARY.md
@src/claude_teletype/tui.py
@src/claude_teletype/bridge.py
@tests/test_tui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session state, turn formatting, and status bar to TUI</name>
  <files>src/claude_teletype/tui.py</files>
  <action>
    **Session state (add to __init__):**
    - `self._session_id: str | None = None` -- captured from StreamResult, passed on subsequent calls
    - `self._turn_count: int = 0` -- incremented on each prompt submission
    - `self._proc_holder: list = []` -- mutable container for subprocess reference from bridge
    - `self._model_name: str = "--"` -- extracted from StreamResult model_usage
    - `self._context_pct: str = "--"` -- calculated from StreamResult model_usage

    **Turn formatting (modify on_input_submitted):**
    Per user decision: transcript-style with "You: " and "Claude: " labels, blank line separation.
    - Increment `self._turn_count`
    - If turn_count > 1, write a blank line to log (turn separator)
    - Write `"You: {prompt}\n\n"` to log
    - Write `"Claude: "` to log (response label, no newline -- response text follows immediately)
    - Write same format to transcript and printer destinations (character by character for printer, bulk for transcript)
    - Replace the old `"\n> {prompt}\n\n"` format entirely

    **Update on_input_changed for printer:**
    - Change the printer prefix from `"\n> "` to `"\nYou: "` to match the new turn format
    - This ensures what the user types on the printer matches the turn label format

    **Status bar (modify compose and add update method):**
    Per user decision: Static widget docked below input, shows "Turn N | Context: X% | model-name".
    - Add `from textual.widgets import Static` import
    - In compose(), yield `Static("Turn 0 | Context: -- | --", id="status-bar")` between Log and Input
    - CSS for status bar: `#status-bar { dock: bottom; height: 1; background: $surface; color: $text-muted; padding: 0 1; }`
    - Add `_update_status()` method that updates the Static widget text using `self.query_one("#status-bar", Static).update(...)`
    - Call `_update_status()` after StreamResult is processed in stream_response (between turns only, NOT during streaming per user decision)

    **Compose order (per research):** Header, Log, Static (status-bar), Input, Footer. The dock:bottom widgets stack from bottom up.
  </action>
  <verify>
    ```bash
    cd /Users/mit/Documents/claude-teletype && uv run python -c "from claude_teletype.tui import TeletypeApp; print('Import OK')"
    ```
    Module imports without errors.
  </verify>
  <done>TUI has session state fields, transcript-style turn formatting with "You: "/"Claude: " labels, and a Static status bar widget showing turn count, context %, and model name.</done>
</task>

<task type="auto">
  <name>Task 2: Add input blocking, escape cancel, and multi-turn streaming loop</name>
  <files>src/claude_teletype/tui.py, tests/test_tui.py</files>
  <action>
    **Input blocking (modify on_input_submitted and stream_response):**
    Per user decision: disable + dim input during streaming.
    - Add CSS: `#prompt:disabled { opacity: 70%; }`
    - In on_input_submitted, after clearing input: `input_widget.disabled = True`
    - In stream_response finally block: `input_widget.disabled = False` then `input_widget.focus()`
    - The existing placeholder change to "Thinking..." stays (user decision: reuse Phase 1 thinking indicator)

    **Escape cancel (add binding and action):**
    Per user decision: Escape cancels streaming, appends "[interrupted]".
    - Add Binding: `Binding("escape", "cancel_stream", "Cancel", show=False)`
    - Add `action_cancel_stream()` method: iterate `self.workers`, cancel any that are not finished
    - In stream_response, catch `asyncio.CancelledError`: write " [interrupted]" to log (note: space before bracket for readability after partial response), then re-raise
    - In stream_response finally block: kill subprocess via `_kill_process()` if `self._proc_holder` is populated

    **Kill-with-timeout subprocess cleanup (add _kill_process method):**
    Per research: SIGTERM -> wait 5s -> SIGKILL
    - `async def _kill_process(self) -> None`: if proc_holder has a process and returncode is None, call proc.terminate(), then asyncio.wait_for(proc.wait(), timeout=5.0), on TimeoutError call proc.kill() + await proc.wait(). Clear proc_holder at end.
    - Also call `_kill_process()` in `on_unmount()` for app exit cleanup.

    **Multi-turn streaming loop (modify stream_response):**
    - Import StreamResult from bridge at top of worker (lazy import pattern)
    - Import calc_context_pct and extract_model_name from bridge (lazy import)
    - Pass `session_id=self._session_id` and `proc_holder=self._proc_holder` to stream_claude_response
    - In the async for loop, check `isinstance(item, StreamResult)`:
      - If StreamResult: store `item.session_id` on `self._session_id`, extract model name and context %, call `_update_status()`
      - If str: pace characters as before
    - After streaming loop (before finally), write "\n" to log to end Claude's response

    **Update tests (tests/test_tui.py):**
    - Update test_prompt_echoed_to_log: check for "You: test prompt" instead of "> test prompt"
    - Add test_status_bar_exists: verify Static widget with id "status-bar" is present in layout
    - Add test_input_disabled_during_streaming: submit prompt, verify input_widget.disabled is True before streaming completes (mock stream_claude_response)
    - Add test_escape_binding_exists: verify "escape" is in app bindings
    - Update test_typing_sends_chars_to_printer: change expected prefix from "\n> " to "\nYou: "
  </action>
  <verify>
    ```bash
    cd /Users/mit/Documents/claude-teletype && uv run pytest tests/test_tui.py -v
    ```
    All tests pass including new ones.
  </verify>
  <done>Input is disabled and dimmed during streaming. Escape key cancels streaming with "[interrupted]" suffix. Subprocess is cleaned up with kill-with-timeout. Multi-turn loop passes session_id between turns via StreamResult. All TUI tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/mit/Documents/claude-teletype && uv run pytest tests/ -v
```
All tests across all modules pass. No regressions in existing bridge, pacer, output, printer, audio, transcript, or TUI tests.
</verification>

<success_criteria>
- User sees "You: " before their prompt and "Claude: " before Claude's response in the TUI output
- Blank line separates conversation turns (not first turn)
- Status bar shows "Turn N | Context: X% | model-name" at bottom of TUI
- Input widget is disabled (dimmed at 70% opacity) while Claude streams
- Escape key cancels streaming and appends " [interrupted]" to output
- Subprocess is killed with SIGTERM->5s->SIGKILL on cancel
- Session_id is captured from StreamResult and passed on subsequent prompts
- Printer and transcript output use same "You: "/"Claude: " format
- All existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-turn-conversation-foundation/05-02-SUMMARY.md`
</output>
