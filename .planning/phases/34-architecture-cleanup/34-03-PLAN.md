---
phase: 34-architecture-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/claude_teletype/printing/profiles.py
  - src/claude_teletype/printing/catalog/__init__.py
  - src/claude_teletype/printing/catalog/juki.py
  - src/claude_teletype/printing/catalog/ibm.py
  - src/claude_teletype/printing/catalog/hp.py
  - src/claude_teletype/printing/catalog/citizen.py
  - src/claude_teletype/printing/catalog/epson.py
  - src/claude_teletype/printing/catalog/oki.py
  - src/claude_teletype/printing/catalog/panasonic.py
  - src/claude_teletype/printing/catalog/tally.py
  - tests/test_profiles.py
autonomous: true
requirements: [ARCH-CLEAN-02]
must_haves:
  truths:
    - "Adding a printer family = adding exactly one catalog/<family>.py file — no edits to profiles.py or any import tuple"
    - "Every profile name resolvable before this plan is resolvable after it, with byte-for-byte identical field values"
    - "Alias blocks live in the catalog module of the family they derive from — no position-dependent module-level ordering in profiles.py"
  artifacts:
    - path: "src/claude_teletype/printing/profiles.py"
      provides: "pkgutil-based _load_catalog; BUILTIN_PROFILES literal holds only 'generic'"
      contains: "pkgutil"
    - path: "src/claude_teletype/printing/catalog/juki.py"
      provides: "juki-6100, juki-2200 profiles + 'juki' alias"
      contains: "juki-6100"
    - path: "src/claude_teletype/printing/catalog/ibm.py"
      provides: "ppds profile + ibm and lexmark-forms aliases"
      contains: "ppds"
  key_links:
    - from: "src/claude_teletype/printing/profiles.py"
      to: "src/claude_teletype/printing/catalog"
      via: "pkgutil.iter_modules(catalog.__path__) inside _load_catalog (function-local import, cycle-break preserved)"
      pattern: "iter_modules"
    - from: "src/claude_teletype/printing/catalog/oki.py"
      to: "src/claude_teletype/printing/catalog/epson.py"
      via: "explicit sibling import for the oki-ml-epson replace(escp) alias"
      pattern: "from claude_teletype.printing.catalog import epson"
---

<objective>
Finish the catalog migration (ARCH-03): _load_catalog auto-discovers catalog modules via pkgutil, the seven inline families move out of the BUILTIN_PROFILES literal into per-family catalog modules, and the nine dataclasses.replace alias blocks move into the module of the family they derive from — killing the hand-edited import tuple and the "AFTER the literal, BEFORE the aliases" ordering constraint.

Purpose: the next printer family is exactly one new file.
Output: profiles.py reduced to the PrinterProfile dataclass + "generic" + loaders + resolve_style; six new catalog modules; two extended ones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.6-ARCHITECTURE-REVIEW.md
@src/claude_teletype/printing/profiles.py
@src/claude_teletype/printing/catalog/__init__.py
@src/claude_teletype/printing/catalog/epson.py
@src/claude_teletype/printing/catalog/oki.py

<interfaces>
Catalog module contract (existing, from catalog/__init__.py docstring): each module exports `PROFILES: dict[str, PrinterProfile]`, imports PrinterProfile from claude_teletype.printing.profiles at module top (safe because profiles.py imports the catalog only function-locally), and every byte sequence carries its citation comment verbatim (R022 — bytes and comments MUST move unaltered).

Existing catalog keys (must not collide with moved families): epson.py {escp2, epson-tm}; oki.py {oki-microline-native}; star.py {star-line}.

Family → destination map:
- juki-6100, juki-2200 literals + "juki" alias → catalog/juki.py (new)
- ppds literal + "ibm" alias + "lexmark-forms" alias → catalog/ibm.py (new)
- escp literal → catalog/epson.py (existing — add to its PROFILES)
- pcl literal → catalog/hp.py (new)
- oki-3390 literal + "oki-ml-ibm" (replace ppds) + "oki-ml-epson" (replace escp) → catalog/oki.py (existing)
- citizen-cts2000 literal → catalog/citizen.py (new)
- panasonic-kxp-epson (replace escp) + panasonic-kxp-ibm (replace ppds) → catalog/panasonic.py (new)
- tally-epson (replace escp) + tally-ibm (replace ppds) → catalog/tally.py (new)
- "generic" stays in profiles.py (not a hardware family; it is the fallback identity)

Cross-family aliases use explicit sibling imports (e.g. in oki.py: `from claude_teletype.printing.catalog import epson as _epson` then `dataclasses.replace(_epson.PROFILES["escp"], ...)`) — Python import resolution handles ordering; pkgutil discovery order becomes irrelevant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pkgutil auto-discovery in _load_catalog + snapshot regression test</name>
  <files>src/claude_teletype/printing/profiles.py, tests/test_profiles.py</files>
  <read_first>src/claude_teletype/printing/profiles.py lines 325-346 (_load_catalog + merge ordering comment); catalog/__init__.py docstring; v1.6-ARCHITECTURE-REVIEW.md ARCH-03</read_first>
  <behavior>
    - Test 1 (write FIRST, against current code): `test_catalog_discovery_covers_all_modules` — the merged BUILTIN_PROFILES contains every key of every catalog module's PROFILES dict, enumerated dynamically via pkgutil.iter_modules over catalog.__path__ (the test does its own discovery, so it automatically covers modules added in Task 2).
    - Test 2 (write FIRST): `test_builtin_profile_names_snapshot` — `sorted(BUILTIN_PROFILES)` equals the exact hardcoded list of names present today (capture it by running `python3 -c "from claude_teletype.printing.profiles import BUILTIN_PROFILES; print(sorted(BUILTIN_PROFILES))"` BEFORE editing). This is the no-profile-lost gate for the whole plan.
  </behavior>
  <action>
    Rewrite `_load_catalog` to discover modules instead of naming them:

    ```
    def _load_catalog() -> dict[str, PrinterProfile]:
        import importlib
        import pkgutil
        from claude_teletype.printing import catalog

        merged: dict[str, PrinterProfile] = {}
        for info in sorted(pkgutil.iter_modules(catalog.__path__), key=lambda i: i.name):
            module = importlib.import_module(
                f"claude_teletype.printing.catalog.{info.name}"
            )
            merged.update(module.PROFILES)
        return merged
    ```

    Keep the imports function-local and preserve the existing cycle-break docstring (importing catalog at profiles.py module top crashes at load time — the warning must survive the rewrite). Sorted iteration makes merge order deterministic; name collisions across catalog modules are a bug, and the snapshot test pins the full key set.

    Note for frozen builds: verify pkgutil.iter_modules works under the PyInstaller onedir spec's collected package (packaging/ smoke script exists); if the smoke check cannot run locally, record it in the SUMMARY as a note — do NOT hardcode a module list as a fallback.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_profiles.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pkgutil' src/claude_teletype/printing/profiles.py` >= 1
    - `grep -v '^\s*#' src/claude_teletype/printing/profiles.py | grep -c 'from claude_teletype.printing.catalog import epson'` == 0 (hand-edited import tuple gone)
    - Snapshot + discovery tests pass against the still-inline profile set
  </acceptance_criteria>
  <done>_load_catalog names no modules; adding a catalog file requires zero profiles.py edits; the full profile name set is snapshot-pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Move inline families and alias blocks into catalog modules</name>
  <files>src/claude_teletype/printing/profiles.py, src/claude_teletype/printing/catalog/juki.py, src/claude_teletype/printing/catalog/ibm.py, src/claude_teletype/printing/catalog/hp.py, src/claude_teletype/printing/catalog/citizen.py, src/claude_teletype/printing/catalog/epson.py, src/claude_teletype/printing/catalog/oki.py, src/claude_teletype/printing/catalog/panasonic.py, src/claude_teletype/printing/catalog/tally.py</files>
  <read_first>src/claude_teletype/printing/profiles.py lines 127-450 (the literals and all nine alias blocks — every citation comment moves with its bytes); src/claude_teletype/printing/catalog/epson.py and oki.py (existing module shape to match)</read_first>
  <action>
    Move profile definitions per the destination map in <interfaces>, byte-for-byte and comment-for-comment (R022: citations travel with their bytes; do not rewrite, reformat, or "improve" any byte literal or citation comment). Rules:

    1. Each new module follows the existing catalog module shape: family docstring, `from claude_teletype.printing.profiles import PrinterProfile` at module top, `PROFILES: dict[str, PrinterProfile] = {...}`.
    2. Aliases built with dataclasses.replace go in the module of the family they DERIVE FROM when same-family (ibm/lexmark-forms in ibm.py; juki alias in juki.py), and in the module of the BRAND when cross-family (oki-ml-ibm/oki-ml-epson in oki.py; panasonic-* in panasonic.py; tally-* in tally.py). Cross-family derivations import the sibling module explicitly, e.g. in oki.py:

    ```
    import dataclasses
    from claude_teletype.printing.catalog import epson as _epson
    from claude_teletype.printing.catalog import ibm as _ibm
    ...
    PROFILES["oki-ml-epson"] = dataclasses.replace(
        _epson.PROFILES["escp"], name="oki-ml-epson", ..., usb_vendor_id=None,
    )
    ```

    Sibling imports are safe (no cycle: catalog modules import only profiles.py's dataclass, and profiles.py imports catalog only function-locally). Preserve every usb_vendor_id=None nulling and its D007 comment — dropping one re-introduces the VID-collision the comments document.
    3. profiles.py after the move: BUILTIN_PROFILES literal contains ONLY "generic"; delete all alias blocks and the "AFTER the literal, BEFORE the aliases" ordering comment (constraint no longer exists — replace the comment with one line: catalog merge extends the generic-only literal; aliases live in their family modules). `BUILTIN_PROFILES.update(_load_catalog())` stays.
    4. Update catalog/__init__.py docstring: add that modules are auto-discovered via pkgutil (one new file = one new family) and that cross-family aliases import siblings explicitly.
    5. LOCKED CONSTRAINTS: dataclasses.replace remains the aliasing mechanism; load_custom_profiles and its three TOML decoding conventions untouched; get_profile untouched.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_profiles.py tests/test_registry.py tests/test_printer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"juki-6100": PrinterProfile' src/claude_teletype/printing/profiles.py` == 0, likewise for escp/ppds/pcl/oki-3390/citizen-cts2000 keys — only `"generic": PrinterProfile(` remains in the literal
    - `grep -v '^\s*#' src/claude_teletype/printing/profiles.py | grep -c 'dataclasses.replace'` == 0
    - `test_builtin_profile_names_snapshot` passes UNMODIFIED from Task 1 (zero names lost or renamed)
    - Byte fidelity: all existing profile-byte assertions in tests/test_profiles.py and tests/test_printer.py pass unmodified; the detection-hint integrity test (tests/test_profiles.py ~1284) passes unmodified
    - Full suite: `python3 -m pytest tests -q` — 0 failures, >= 965 collected
  </acceptance_criteria>
  <done>One home for family data: profiles.py defines the dataclass and "generic"; every family and alias lives in exactly one catalog module; adding a family is one new file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog module discovery | pkgutil imports every .py in the catalog package — same trust domain as the package itself (first-party source, no user-writable path); no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Tampering | byte drift during profile moves | mitigate | snapshot test + existing byte-pinning tests must pass unmodified; citations move verbatim |
</threat_model>

<verification>
- `python3 -m pytest tests -q` — full suite green, >= 965 collected
- `python3 -c "from claude_teletype.printing.profiles import BUILTIN_PROFILES; print(len(BUILTIN_PROFILES))"` prints the same count as before the plan
</verification>

<success_criteria>
Success criterion 2 (ARCH-03) closed: pkgutil auto-discovery, zero hand-edited import tuples, all families and aliases in catalog modules, identical profile set byte-for-byte, full suite green.
</success_criteria>

<output>
Create `.planning/phases/34-architecture-cleanup/34-03-SUMMARY.md` when done.
</output>
