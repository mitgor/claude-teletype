---
phase: 02-terminal-simulator
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/claude_teletype/cli.py
autonomous: false

must_haves:
  truths:
    - "Running `claude-teletype` with no arguments launches the TUI (split-screen mode)"
    - "Running `claude-teletype --no-tui 'prompt'` preserves Phase 1 behavior (stdout, no TUI)"
    - "Piped input (echo 'hi' | claude-teletype) auto-falls back to --no-tui mode"
    - "User can type a prompt in the TUI and see Claude's response appear character by character in the top pane"
    - "Character pacing in the TUI feels identical to Phase 1 stdout pacing"
  artifacts:
    - path: "src/claude_teletype/cli.py"
      provides: "Updated CLI entry point with TUI as default mode"
      contains: "TeletypeApp"
  key_links:
    - from: "src/claude_teletype/cli.py"
      to: "src/claude_teletype/tui.py"
      via: "import TeletypeApp and call .run() as default mode"
      pattern: "from claude_teletype\\.tui import TeletypeApp"
    - from: "src/claude_teletype/cli.py"
      to: "sys.stdin.isatty()"
      via: "piped stdin detection for auto --no-tui fallback"
      pattern: "sys\\.stdin\\.isatty"
---

<objective>
Update the CLI entry point to use the TUI as the default mode and verify the full experience end-to-end.

Purpose: This wires the Textual TUI (from Plan 02-01) into the entry point so `claude-teletype` launches the split-screen simulator by default. Phase 1 behavior is preserved via `--no-tui` flag. This completes Phase 2 by making the TUI the primary user experience.

Output: Updated `cli.py`, human-verified TUI experience
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-simulator/02-RESEARCH.md
@.planning/phases/02-terminal-simulator/02-01-SUMMARY.md

@src/claude_teletype/cli.py
@src/claude_teletype/tui.py
@src/claude_teletype/pacer.py
@src/claude_teletype/bridge.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI entry point with TUI as default mode</name>
  <files>src/claude_teletype/cli.py</files>
  <action>
Update `src/claude_teletype/cli.py`:

1. **Change the `prompt` argument from required to optional:**
   - `prompt: str = typer.Argument(None, help="Prompt (omit for interactive TUI)")`

2. **Add `--no-tui` flag:**
   - `no_tui: bool = typer.Option(False, "--no-tui", help="Disable TUI, use plain stdout (Phase 1 mode)")`

3. **Add piped stdin detection** at the top of the `chat` function:
   ```python
   import sys
   if not sys.stdin.isatty():
       no_tui = True
   ```

4. **Route to correct mode:**
   ```python
   if no_tui:
       if not prompt:
           console.print("[bold red]Error: prompt required with --no-tui")
           raise typer.Exit(1)
       asyncio.run(_chat_async(prompt, delay))
   else:
       from claude_teletype.tui import TeletypeApp
       tui_app = TeletypeApp(base_delay_ms=delay)
       tui_app.run()
   ```
   Key: `TeletypeApp` import is lazy (inside the branch) so Textual is not loaded in --no-tui mode.

5. **Keep `_chat_async` function unchanged** -- it is the Phase 1 fallback path.

6. **Move `import sys` to module level** (it is already used by the pacer, but cli.py needs it for isatty check).

Run: `uv run ruff check src/claude_teletype/cli.py` -- clean.
Run: `uv run pytest tests/ -v` -- all tests still pass (no regressions).
  </action>
  <verify>
`uv run ruff check src/claude_teletype/cli.py` exits 0.
`uv run pytest tests/ -v` passes all tests.
`claude-teletype --help` shows both `--no-tui` and `--delay` options, with `PROMPT` as optional argument.
  </verify>
  <done>
CLI entry point uses TUI as default mode (no arguments needed), preserves Phase 1 behavior with --no-tui flag, and auto-detects piped stdin to fall back to non-TUI mode.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify TUI experience end-to-end</name>
  <files>src/claude_teletype/cli.py</files>
  <action>
    Human verification of the complete Phase 2 terminal simulator. Claude has built a split-screen TUI with typewriter-paced Claude responses, input handling, and --no-tui fallback.

    Steps for user to verify:

    1. **TUI mode (default):**
       - Run `claude-teletype` with no arguments
       - Verify: Split-screen appears (Header at top, output area in middle, input at bottom, Footer)
       - Type a prompt (e.g., "What is 2+2?") and press Enter
       - Verify: Input clears, "> What is 2+2?" appears in top pane
       - Verify: "Thinking..." placeholder appears in input while waiting
       - Verify: Claude's response appears character by character with typewriter pacing in top pane
       - Verify: Pacing feels the same as Phase 1 (punctuation slower, spaces faster)
       - Verify: After response completes, input placeholder resets and you can type another prompt
       - Press Ctrl+D or Ctrl+C to exit

    2. **--no-tui fallback (Phase 1 mode):**
       - Run `claude-teletype --no-tui "What is 2+2?"`
       - Verify: Same behavior as Phase 1 (thinking spinner, then character-by-character stdout output, no TUI)

    3. **Custom delay:**
       - Run `claude-teletype --delay 30` and type a prompt
       - Verify: Characters appear faster than default

    4. **Edge case:**
       - Try submitting an empty prompt (just press Enter) -- nothing should happen

    Resume signal: Type "approved" if the TUI experience feels right, or describe what needs adjustment.
  </action>
  <verify>User confirms split-screen TUI works: characters appear one at a time in top pane, input works in bottom pane, pacing matches Phase 1, and --no-tui fallback preserves Phase 1 behavior.</verify>
  <done>User has approved the TUI experience. Phase 2 success criteria are met: split-screen layout, interactive input, typewriter-paced output, and mirrored output architecture ready for printer integration.</done>
</task>

</tasks>

<verification>
1. `claude-teletype` launches TUI with split-screen layout
2. Prompt input -> typewriter-paced response in output pane
3. `claude-teletype --no-tui "hello"` works like Phase 1
4. All tests pass: `uv run pytest tests/ -v`
</verification>

<success_criteria>
- TUI launches as default mode with no arguments
- User can type prompts and see Claude's typewriter-paced responses
- Phase 1 fallback works via --no-tui flag
- Character pacing matches Phase 1 timing
- Human verified the complete experience
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-simulator/02-02-SUMMARY.md`
</output>
