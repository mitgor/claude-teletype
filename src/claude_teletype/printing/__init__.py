"""Printing package: drivers, discovery, selection, detection, profiles,
registry, catalog, pipeline.

Thin namespace package — import the submodules directly. Canonical patch
targets are ``claude_teletype.printing.{discovery,selection,detection,drivers}``.
"""
