"""Screens package: Textual Screen/ModalScreen modules.

Thin namespace package — import the submodules directly
(``claude_teletype.screens.printer_setup`` etc.). Note the old
``*_screen`` suffix is dropped here; top-level ``*_screen.py`` modules
remain as re-export shims for the pre-split import paths.
"""
